# 타이어모어 업무 자동화 프로젝트

## 이 문서의 목적
Claude가 매 세션 시작 시 읽는 프로젝트 기억 파일. 대화가 끊겨도 여기만 읽으면 맥락이 복구된다.
새로운 결정이 나올 때마다 아래 **결정 기록**에 추가할 것.

---

## 개발 명령어 (`C:\dev\tyremore` 에서 — 여기가 저장소다)

| 할 일 | 명령 |
|---|---|
| 개발 서버 | `npm run dev` |
| 타입 검사 | `npm run typecheck` — **갈래(subagent)는 여기까지만** |
| 테스트 전부 | `npm test` (`src/lib/*.test.ts` 26개) |
| **테스트 하나만** | `npx tsx --test src/lib/<이름>.test.ts` |
| 한꺼번에 점검 | `npm run check` (`scripts/check-all.ts`) |
| 운영 빌드 | `npm run build` — 산출물은 **`.next-prod`** |
| DB 스키마 반영 · 들여다보기 | `npm run db:push` · `npm run db:studio` |
| 일회성 스크립트 | `npx tsx --env-file=.env.local scripts/<이름>.ts` (대개 `--dry` 로 먼저 본다) |

- 🔴 **개발 `.next` 와 운영 `.next-prod` 는 절대 합치지 않는다.** `next.config.ts` 가 자동으로
  가른다. 같이 쓰면 청크가 섞여 **CSS·버튼·모듈이 한꺼번에 죽는다**(2026-08-01 실제로 겪음).
- 빌드가 `WasmHash` 로 죽으면 캐시 손상이다 → `.next-prod` 를 지우고 다시. `EPERM` 은
  `esbuild.exe` 잠김이니 **그 프로세스만** 종료한다.
- 🔴 `npm run import`(`scripts/import/run-all.ts`)는 **다시 돌리지 않는다** — 손으로 고친
  이름·규격이 덮인다 (D-08). 코드로도 막아 두었다.
- 배포 = `git push`. Vercel 이 1~2분 안에 매장 태블릿까지 반영한다 (D-13).

---

## 정본 지도 — **판정은 한 곳에만 둔다**

이 프로젝트에서 같은 판정을 두 곳에 두면 반드시 갈라진다. 실제로 외상 대상 판정은 세 곳에
복붙돼 있었고, 순위 배지·집계가 화면과 보고서에서 다른 말을 했다. **아래 파일이 그 판정의
유일한 주인이다 — 고칠 일이 생기면 여기만 고치고, 화면·스크립트·갈래는 전부 이걸 불러 쓴다.**

| 영역 | 정본 (`src/lib/`) | 무엇을 정하나 |
|---|---|---|
| 마진 | `margin-def.ts` | 공임 전액 마진 · 소모품 차감 · 모르는 원가 제외 |
| 뒷마진 | `rebate.ts` | 월별 장려금 층, 0원 원칙 (D-28) |
| 재고 | `stock-integrity.ts` · `stock-catchup.ts` | 정합 판정 · 미차감 소급 (D-26) |
| 고객 | `normalize.ts` | 이름·전화 정규화, 더미는 매칭 금지 (D-27) |
| 외상 | `receivable-key.ts` | 「누구에게 받을 돈인가」 (D-29) |
| 결제 | `payments.ts` · `pos-vocab.ts` · `reservation-pay.ts` | 수단 분류 · POS 낱말 · 예약금 (D-18·D-20) |
| 회계 입력 | `fin-sheet.ts` | 엑셀 7종 파서 (D-35) |
| 통장 대조 | `tax-recon.ts` (`bankLabel`) | 부호는 금액 칸에서 (D-35) |
| 계산서 | `tax-book.ts` | `taxBook(ym)` 하나 (D-23) |
| 돈관리 첫 화면 | `today-brief.ts` · `weekly-steps.ts` · `invoice-deadline.ts` · `receivable-total.ts` | 오늘·주간·10일 경고·미수금 (D-22) |
| 돈관리 말 | `fin-words.ts` | 화면 글자는 여기서만 (D-24) |
| 장부 조회 | `ledger-data.ts` | `/finance/ledger` 의 SQL — 화면에 인라인 금지 (D-39) |
| 연간 실행 | `year-run.ts` | 한 달 진행 엔진 (D-35) |
| 상품 | `kumho-sheet.ts` · `conti-sheet.ts` · `conti-name.ts` | 거래처 목록 적재·이름 규칙 (D-17·D-19) |
| 배터리 | `battery-price-list.ts` | 단가표 상수 — 화면·스크립트가 같이 본다 (D-25) |
| MARS | `mars-queue.ts` · `mars-core.ts` · `mars-ready.ts` · `mars-audit.ts` · `mars-service-words.ts` | 대기열·도장·필수정보·감사·점검표 낱말 (D-34) |
| 차량 사진 | `vin.ts` · `vin-photo-core.ts` · `agent-version.ts` | VIN 한계 · 읽은 것 검사 · 대리인 판 번호 (D-31) |
| 블로그 | `blog-style.ts` · `blog-form.ts` · `blog-gemini.ts` · `ai-cli.ts` | 문체·재료·다듬기 지시문·CLI 옵션 (D-33) |
| 화면 | `src/components/ui/` · `src/app/globals.css` | 부품 12개 · 색 토큰 (D-32) |

> 🔴 **새 판정을 만들 때도 같은 규칙이다.** 화면 파일 안에 SQL 이나 분기를 새로 심지 말고
> `src/lib/` 에 함수로 두고 부른다 — 나중에 스크립트·갈래·보고서가 같은 답을 해야 한다.

---

## Claude의 역할
- **1단계 (완료)**: 업무분석가(BA) 겸 제품기획자 — 현황 분석, MVP 범위 확정
- **2단계 (완료 · 2026-07-31)**: 솔루션 아키텍트 — 기술 스택, 데이터 모델, 화면 설계
  → 산출물: `docs/08-아키텍처-확정.md` · `09-데이터모델-확정.md` · `10-화면설계.md`
- **3단계 (완료 · 2026-08 오픈)**: 풀스택 개발자 — 구현·이관. 매장에서 실제로 쓰기 시작했다
- **4단계 (현재)**: 운영 중 개선 — 현장에서 나온 사고·불편을 고치고, 고친 이유를 여기 남긴다

> ⚠️ **지금은 「만드는 중」이 아니라 「돌아가는 중」이다.** 매장이 오늘 장사에 쓰는 앱이므로
> ① 판매·결제·재고를 건드리는 변경은 **되돌릴 길을 같이** 만들고
> ② 자료를 고치는 스크립트는 **`--dry` 로 먼저 보여드리고 승인받은 뒤** 돌린다
> ③ 사고가 나면 **원인과 함께 결정 기록에 남긴다** — 같은 자리에서 두 번 데지 않게.

사용자는 **비개발자(업체 운영자)** 이다. 기술 용어는 풀어서 설명하고, 선택지를 줄 때는
"현장에서 무엇이 달라지는가"를 기준으로 설명할 것. 추천안을 반드시 명시할 것.
🔴 **승인을 구할 때 코드를 보여주지 않는다** — 「이 버튼을 누르면 X 대신 Y 가 됩니다」로 적는다.

---

## 업체 프로필

| 항목 | 내용 |
|---|---|
| 업체명 | 타이어모어 |
| 업종 | 타이어 판매 + 경정비 |
| 월 타이어 판매량 | 300~800본 (일 10~30본) |
| 사용자 | 사장님 1명 + 정비사 2~4명 |
| **소속** | **미쉐린(Michelin) 프랜차이즈 가맹점** |
| 현재 도구 | **MARS**(미쉐린 본사 시스템) + 엑셀/구글시트 + 수기 장부·전표 → **삼중 관리** |
| 부가 사업 | ① 타이어 보관 서비스 ② 온라인 제휴 물량 ③ 법인·거래처 외상 거래 |
| 보유/구매가능 장비 | 사무실 PC·노트북, 직원 개인 스마트폰, 매장용 태블릿, 라벨프린터·바코드스캐너 |

---

## 핵심 고충 (사용자가 직접 꼽은 순서)
1. **타이어 재고 확인·발주** — 무엇이 몇 본 있는지 즉시 안 나옴
2. **고객 상담 중 비용 확인** — 고객을 세워둔 채 가격을 계산해야 함 ← 매출 직결
3. **고객·차량 정비이력 조회 및 입력**
4. **견적·정산·매출 집계**
5. **타이어 보관 서비스 전반** — 위치·목록·만료청구·재방문유도 **4개 항목 전부 문제** (사실상 미구축)
6. 제휴 정산금 대조가 번거로움

> 법인 외상 청구서·미수금 관리는 **문제로 꼽지 않음** → 우선순위 후순위

---

## 결정 기록 (Decision Log)

### D-01. 오픈 순서: A안 (재고 → 상담·견적)
1개월 내 1차 오픈. 4주 계획은 `docs/02-MVP-정의서.md` 참조.
이후 2개월차 보관관리 → 3개월차 작업지시·정비이력 → 4개월차 매출·정산·제휴대조.
**이유**: 상품·가격 마스터가 나머지 전부의 토대라 헛돈이 안 됨 + 고충 2순위(상담 중 견적)를 직접 해결.

### D-02. 재고는 1본 단위로 관리한다
DOT/시리얼까지 실물 추적하는 것이 목표. 단, 입력 부담 때문에 단계적으로 접근:
- **입고 시**: 제조사 바코드를 바코드리더기로 스캔 → 상품(SKU) 자동 인식 → 수량 입력 →
  시스템이 개별 1본짜리 재고 레코드를 자동 채번 생성
- **DOT**: 최초 입고 시 필수 아님. 나중에 보완 입력 가능한 필드로 설계
- 자체 QR 라벨은 **재고 타이어에는 붙이지 않음**

> ⚠️ 제조사 바코드는 SKU 코드라서 같은 상품 4본은 바코드가 전부 동일하다.
> 개별 본을 물리적으로 특정할 수 없으므로, 개별 식별이 꼭 필요한 곳(보관)은 D-03으로 해결.

### D-03. 보관 타이어는 등록 시 보관번호 스티커를 출력해 부착
라벨프린터로 보관번호 + 고객명 + 차량번호 스티커 1장 출력 → 타이어 묶음에 부착.
랙·열·단 위치는 시스템에 기록. 재고 타이어에는 라벨 불필요.

### D-04. 차량번호 조회는 자사 이력 DB로만 한다 *(2026-07-31 2차 개정)*
- **자사 DB 조회 단일 방식.** 재방문 고객은 차량번호만 치면 전부 뜬다 — 무료, 즉시.
- **외부 유료 API 도입 계획 없음.** (기존 PartZone 병행안은 폐기)
- 신규 고객은 **최초 1회만 차종·규격을 사람이 입력**한다. 그 뒤로는 자사 DB가 답한다.
  → 장착한 타이어 규격이 곧 그 차의 규격이므로, **판매 자체가 데이터 수집이다.**
- MARS 이관 데이터가 있으므로 오픈 시점에 이미 기존 고객은 커버된다.
- 외부 조회가 나중에 필요해질 경우를 대비해 **인터페이스 자리만** 남겨둔다 (미구현).

> ⚠️ 차량번호 → 차종·연식 조회는 자동차등록정보라 공공 무료 API로 열리지 않는다.
> 유료 상용 API 외엔 경로가 없고, 그것을 안 쓰기로 했으므로 **신규 차량은 수기 입력이 원칙**이다.

#### D-04 3차 개정 — 순정 제원표를 둔다 *(2026-09-03, 사장님 요청)*

**바뀌지 않는 것**: 차량번호 → 차종 **조회**에 외부 유료 API 를 쓰지 않는다. 신규 차량은 여전히
사람이 입력한다. 판매 기본값도 끝까지 `last_fitted_size` 다. 이 셋은 그대로다.

**바뀌는 것**: 「`oem_tire_size` 는 두지 않는다」를 뒤집는다. 단 `vehicle` 행에 컬럼으로 붙이지
않고 **`vehicle_spec` 별표에 근거와 함께** 둔다.

왜 지금 뒤집는가:
- 대체재로 쓰기로 했던 **`last_fitted_size` 가 실측 0건**이다. 대체가 실제로는 없었다.
- 「외부 API」가 아니라 **제조사가 공개한 취급설명서를 사람이 눌러 한 페이지씩 옮겨 적는 것**이다.
  조회 서비스가 아니라 참고자료 표이고, 같은 성격의 표가 이미 있다
  (`kumho_material`·`continental_material` — 둘 다 `source_label` 필수).
- 사장님 요청 원문: 「휠너트 체결 토크, 표준 공기압, 엔진오일 양·점도·규격, 타이어 규격(인치별),
  배터리 규격… **철저한 검증을 통해서 할루시네이션을 방지**하는 등의 여러 조치가 취해져야함.」

🔴 **D-04 의 정신은 그대로 지킨다 — 추측을 차량 행에 굳히지 않는다.**
값에는 반드시 출처 URL·원문 인용·읽은 날짜가 붙고, **사장님이 원문을 보고 승인하기 전에는
숫자를 화면에 보여주지 않는다.** 휠너트 토크처럼 틀리면 사고가 나는 값은 독립된 출처 2개를
요구한다. 자세한 것은 `docs/19-제원-표기-정본.md`.

### D-05. 가격 계산 = 기표가 기준, 매입·판매 할인율 분리, 쓰면서 채움 *(2026-07-31 개정)*
```
              기표가 (MARS 확보 ✅)
        ┌───────────┴───────────┐
   매입 할인율              판매 할인율
        ↓                       ↓
   매입 원가              가이드 판매가 → 고객등급 추가할인
        │                       ↓  → 현장 재량 조정 → 최종 판매가
        └───────────┬───────────┘
              마진 = 최종가 − 매입원가
```
**핵심 결정:**
1. **매입 할인율과 판매 할인율은 다르다.** 별도 필드로 관리한다.
2. **할인율은 사전 입력하지 않는다.** 10,691건을 미리 채울 수 없고 그럴 필요도 없다.
   공란으로 시작 → 상담 화면에서 입력 → "이 상품만/이 모델/이 브랜드에 저장?" 선택
   → **파는 만큼 마스터가 저절로 완성된다.**
3. **할인율 ↔ 판매가 양방향 자동 계산.** 어느 쪽을 입력해도 나머지가 나온다.
   (정비사는 "25% 빼줘"라고도, "54만원"이라고도 생각한다)
4. 적용 우선순위: **개별상품 > 패턴/모델 > 브랜드 > 품목범주.** 좁은 것이 이긴다.
5. **현장 재량 오버라이드는 반드시 허용.** 막으면 직원이 시스템을 안 쓴다.
6. **매입원가·마진은 사장님 계정에만 노출.** 정비사 화면·고객 시야에서 가린다.

> 상세 설계는 `docs/06-가격계산-설계.md`. **할인율 수치 없이도 1주차 착수 가능.**

#### ⭐ D-05 보강 — 기표가는 VAT 포함이다 *(사장님 확인, 2026-08-01)*
- **MARS 「단가1」은 VAT 미포함이다.** 그대로 띄우면 상담 중에 **10% 낮은 금액**을 부른다
- 기표가는 **고객에게 말하는 금액**이므로 세금이 들어 있어야 한다
- `product.list_price` = **VAT 포함** (화면·견적용) / `list_price_excl` = MARS 원본 보존
- 브랜드 단위로 갈린다 → **`brand.price_excludes_vat`**. 켜면 이관·재이관 때 자동 가산
  - ⚠️ 손으로 한 번 고치면 다음 이관에 날아간다. **적재 단계에 규칙을 박아야 한다**
- 미쉐린 2,057건 전부 1,000원 단위 → ×1.1 하면 **전부 100원 단위**. 반올림 손실 0
- ⚠️ **다른 브랜드는 미확인.** 피렐리 1,535건도 전부 1,000원 단위라 같은 성격일 가능성이 높다.
  「상품 정리」 화면에서 브랜드별로 켜면 즉시 재계산된다

### D-06. 클라우드 웹 방식 (향후 자체 서버 이관 가능성 고려)
현재는 클라우드 웹앱. 다만 사용자가 **향후 자체 서버 구축 가능성**을 언급했으므로,
특정 클라우드에 종속되지 않는 표준 스택(PostgreSQL, Docker 이식 가능)으로 설계할 것.

### D-07. 초기 데이터 이관 *(D-08 반영해 개정)*
- 현재 재고 목록: **엑셀 보유** → 일괄 업로드 기능 필요 (1주차)
- 타이어 기표가표: **보유** → 상품·가격 마스터의 기준
- 고객·차량 명단: ~~없음~~ → **MARS에 이미 존재. 엑셀 내보내기로 이관** (1주차)
- 결제·거래 이력: **MARS에 존재** → 내보내기로 이관, 재방문 고객 이력 확보
- 보관 중인 타이어 목록: **없음** → 오픈 시 1회 실사 후 등록 필요

### D-08. MARS(미쉐린 본사 시스템)는 대체 불가. 새 시스템은 MARS를 "먹여야" 한다 ⭐
| 항목 | 확인된 사실 (2026-07-31 화면 검증 완료) |
|---|---|
| 제품 | **incadea.fastfit 10.10.00 (W1)** — incadea사의 타이어·경정비 매장용 상용 패키지 |
| 기반 기술 | **Microsoft Dynamics 365 Business Central** ✅ 확정 |
| 운영 주체 | **타이어모어 코리아 본사** (`mars.tyremore.co.kr`) — 미쉐린 글로벌 아님 ⭐ |
| 인증 | Microsoft Entra ID (`6116858301@mars.tyremore.co.kr`) |
| 테넌트/회사 | `tenant=61168583` / `company=Tyremore Sokcho` (속초점) |
| 우리 권한 | **fastfit Front Office Manager** (매니저 역할) |
| 현재 입력 방식 | **MARS 화면에 건별 수기 입력** ← 삼중 입력의 주범 |
| 입력 대상 | 고객 정보, 업무 내용, 결제 정보 |
| 데이터 내보내기 | **가능** ✅ / **「Excel에서 편집」은 없음** ❌ (양방향 엑셀 경로 폐기) |
| 업로드 주기 | **정해진 주기 없음. 누락만 없으면 됨** ✅ 설계 자유도 높음 |
| 「웹 서비스 설정」 | ❌ BC 표준 웹서비스 페이지 **아님**. MARS가 외부 API를 *호출*하는 설정 화면 |
| 외부 연동 확인 | PartZone(차량사양·부품) + Naver CLOVA OCR — **본사 계약 자산. 우리는 사용하지 않음** |
| **본사 협의** | ❌ **불가** — 본사와 연동 협의를 진행할 수 없음 (2026-07-31 확정). OData 경로 폐기 |
| 🚨 보안 | 해당 화면에 **본사 API 키 평문 노출**. 스크린샷 마스킹 필수, **키 무단 사용 금지** |

#### ⭐ MARS 는 **단방향 출구**다 (사장님 확정 2026-08-04)

#### ⭐ 전기(Posting)까지 자동 (사장님 결정 2026-08-04 — 8/2 결정 번복)

> "사실 전기까지 자동으로 완료가 되어야함. 이유는 전기를 하는 것이 생각보다
> 복잡해서 사람 손이 많이 들어감. 전기, 전기 후 차량 점검 완료까지 원스톱으로."

- 흐름: 매출 주문 채우기 → **합계 대조** → 전기(출하 및 송장) → 전기 후 차량 점검 제출.
- 🔴 안전장치: **합계가 일치할 때만 전기한다.** 어긋나면 초안으로 남기고 메모에 사유를 적는다
  (전기는 취소가 번거롭다 — 대변 전표).
- MARS 미등록 상품(거래처 목록·손 등록으로 만든 235개)은 **범용 품번**으로 넣고 실제
  상품명을 「설명 2」에 남긴다. ⭐ 범용 품번 = **TEST000002** (사장님 지정 2026-08-05,
  "MISC 185/60 R 14 82H TL / TEST TIRE ITEM 01", 10-TIRES) — `.env.local` 의
  `MARS_FALLBACK_ITEM`. `--try-item` 으로 실전 경로(매치 창→대체→수량·단가 검증) 통과.
  (전 후보 `580/001/00290` 은 마스터 미완성이라 거부됐었다 — 2026-08-04 실측.)
- 🔴 모르는 품번을 치면 오류 창이 아니라 **「매치 코드 검색」 창**이 뜬다 (2026-08-05).
  같은 글자가 홈 메뉴에도 있고 **안 보이는 복제 노드**도 있어 감지가 세 번 빗나갔다 —
  창에만 있는 칸 이름(비교 수량·인벤토리 필터)을 정규식으로, **보이는 노드만** 찾는다.
  품번 엔터 후에는 신호(오류 창·매치 창·측정단위 채워짐)가 잡힐 때까지 최대 15초 기다린다.
- 동의는 손님이 고른 그대로 — 거부는 「거부된 동의」 + **불매치 코드 NONEED** (사장님 확인).
  칸 이름은 **`Disagreement Code`** (2026-08-05 실측 — "Mismatch" 후보는 전부 헛짚음).
  🔴 NONEED 를 못 넣으면 **수락으로 되돌리지 않고 등록을 멈춘다** (사장님 지시 2026-08-05 —
  받지 않은 동의를 수락으로 남기는 것이 최악). `--try-consent` 로 저장 없이 검증할 수 있다.
- 🔴 「결제 조건 코드」는 건드리지 않는다 — **결제 수단 코드만** 넣는다 (사장님 지시 2026-08-05).
  조건 코드에 BANK 를 넣었더니 페이지 오류가 걸려 품목 표 자체가 안 떴다.
- ⭐ 주행거리는 **차량의 최근 값**(vehicle.mileage)을 항상 넣는다 (사장님 버그 제보 2026-08-05).
  판매 등록에서 고치면 최신값, 안 고치면 마지막으로 알던 값이 들어간다.
- ⭐ 기존 고객 + **새 차량** (렌트카·차 바꾼 손님): 마스터 데이터 → 차량 → 신규 카드에
  「연락처」(C583-…)를 이어 만든다. 「연락 고객 차량 생성」 창은 새 연락처 전용이고,
  「연락처에 첨부」는 회사 연락처만 받는다 (2026-08-05 탐침).
- 🔴 **갓 만든 차량은 검색 색인에 한동안 안 잡힌다** — 번호판 검색·주문 목록·송장 목록 모두.
  그래서 ① 만든 차량 번호(V583-…)를 우리 DB `vehicle.mars_vehicle_no` 에 바로 기억해
  재실행 시 또 만들지 않게 막고(실제로 한 번 중복 생성됨 — V583-002653), ② 주문은
  번호판이 아니라 **이름+전화** 검색으로 진행한다 (`findCustomerByName`).
- 🔴 전기 성공 판정은 **전기 전/후 화면의 송장 번호 diff** 로만 한다 (2026-08-05 사고 —
  주문 화면 뒤층 고객 이력에 그 손님의 **옛 송장 번호**가 깔려 있어, 전기가 안 됐는데
  옛 번호를 주워 성공으로 기록했다). 전기 대화상자에서 「배송 및 송장」을 못 찾으면
  기본값으로 진행하지 않고 **취소**한다 — 기본값(배송)은 재고만 출하되고 송장이 안 생긴다.
- 채워진 초안에 전기만 이어서: `npx tsx scripts/mars-fill.ts --post-draft <번호판> <합계> [이름] [전화]`
- ⭐ 점검표(전기 후 차량 점검)는 판매 내용을 따른다 (사장님 지시 2026-08-05):
  ① 타이어 교체 바퀴 = 판매 등록의 「갈아 끼운 바퀴」 체크박스(quote.tyre_positions) 그대로,
  안 골랐으면 본수로 짐작. ② 엔진오일·패드·배터리·얼라이먼트를 **실제로 교환·조정**했으면
  100% 대신 **교체 칸** — 서비스 이름으로 판단하고 「점검」 서비스는 제외.

> "MARS 는 실적을 위해서 필수적으로 등록해야하지만 **그것뿐이며**
> MARS 를 그 이상으로 사용할 생각은 없음."

- **내보내기만 한다.** 매출 주문 입력→전기→차량 점검이 전부다 (`scripts/mars-fill.ts`).
  MARS 에서 **읽어 우리 DB 에 되먹이는 코드는 없다.**
- **상품 카탈로그의 주인은 우리다.** 1주차 이관(2026-08-01)이 마지막이었고
  `scripts/import/run-all.ts` 는 **다시 돌리지 않는다** (코드로도 막아 두었다).
  다시 돌리면 손으로 고친 이름·계절·규격과 거래처 목록으로 채운 것이 덮인다.
- 상품이 늘어나는 길은 셋뿐: **거래처 상품목록 올리기** · 인보이스에서 만들기 · 손으로 등록.
  실제로 거래처가 주는 목록이 MARS 수출본보다 정확하고 최신이다
  (2026-08-04 금호 자재검색 459건 중 **233건이 우리에게 없었다**).
- `mars_item_no`·`mars_contact_no`·`mars_vehicle_no`·`mars_service_no` 는 **그대로 둔다** —
  가져오는 값이 아니라 **내보낼 때 맞추는 열쇠**라 이 원칙과 어긋나지 않는다.

### D-09. 상품·가격 마스터는 MARS 내보내기로 확보 완료 ⭐ (2026-07-31)
MARS 「마스터 데이터 → 상품」 내보내기 = **10,691건** (타이어 10,319건).
- **`단가1` 컬럼이 기표가(정가)** — 타이어 79%가 값 보유. **별도 기표가표 불필요**
- 규격 자동 파싱 성공률 **99.0%** (실패 101건만 수동 보정)
- 브랜드 18종 (미쉐린 4,153 / 피렐리 1,535 / 한국 1,399 / 콘티넨탈 927 / 금호 690 …)
- 배터리·오일·브레이크·부품 **372건** → 경정비 마스터도 조기 확보 (단, **공임은 없음**)
- 「보고서 → 가격표」는 비어 있음. 기표가는 상품 마스터에 있었다.

> 결과: **1주차 공수 감소.** 상품 마스터 수작업 구축과 기표가 입력이 통째로 사라졌다.
> 상세: `docs/05-MARS-상품데이터-분석.md`

### D-10. 고객은 「고객」이 아니라 「연락처」다 ⭐⭐ (2026-07-31)
MARS 내보내기 4종(고객·연락처·차량·패스트핏 서비스) 분석 결과.

**핵심 구조 — 이걸 잘못 알면 설계가 통째로 어긋난다:**
```
[회계 축]  고객 D583/000000 "일반 고객(개인)"   ← 개인 매출 전부가 이 한 계정
           고객 D583/000010 "외상 고객(법인)"
                     │
[실무 축]  연락처 C583-xxxxxx "홍길동" ── 차량 V583-xxxxxx "12가3456"
```
- 「고객」 파일은 **2건뿐**. 고객 명단이 아니라 **회계 계정**이다.
- **실제 고객 명단 = 「연락처」 2,603건.** 식별자는 `C583-…`
- → `customer.mars_customer_no` **폐기**, **`mars_contact_no`(C583-…) 사용**
- → MARS 입력 대기열은 "고객 계정"이 아니라 **「연락처」 등록**을 안내해야 한다

| 확보 데이터 | 건수 | 품질 |
|---|---|---|
| 연락처(고객) | 2,603 | **휴대폰 99.6%** ⭐ / 이메일 0건 / 주소 없음 |
| 차량 | 2,574 | 번호판 99.8% · 제조사 99.7% · 모델 96% · 연식 99.8% · 주행거리 99.6% |
| 연락처↔차량 연결 | — | **매칭 실패 0건, 이름 불일치 0건** |
| 패스트핏 서비스 | **69** | ⭐ **공임 단가 포함. 국산/수입 구분 있음** |

**파생 결정:**
1. **D-04(자사 DB만)가 옳았음이 확증됐다.** 제조사·모델·연식이 99% 있다.
   외부 API로 사려던 정보를 이미 갖고 있었다. 없는 건 순정규격뿐인데
   `last_fitted_size`로 대체한다는 판단이 유효하다.
2. **이메일 기능은 설계에서 제외.** 0건이다. 알림은 **문자 단일 경로**.
3. **이름 속 메모(107건)를 지우지 않는다.** `고태환[한진택배]`는 사장님이
   손님을 기억하는 방식이다. 원문 보존 + 괄호 안을 `memo`에 복사, 검색은 둘 다 뒤진다.
4. **휠밸런스는 "2개당" 요금이다.** 4본이면 ×2. 코드에 수량 규칙을 박는다.
   인치 구간이 교환(17/18/21)과 밸런스(17/20/21)로 **다르다** → 규격에서 자동 판정.
5. **공임에 국산/수입 구분이 있다** → 차량 제조사로 **자동 선택**시킨다.
6. 제조사 표기 48종 분산(현대자동차/현대/HYUNDAI) → **매핑으로 25종 통합** 후 이관.

> 상세: `docs/07-MARS-고객차량서비스-분석.md`
> 결과: **3주차 블로커(부대비용)와 2개월차 블로커(보관료)가 조기 해소됐다.**

**MARS 입력 프로세스 (실제)**
```
① 메인화면 「고객 정보 검색」 → ② 고객 유무 확인 / 고객·차량 등록
→ ③ 신규 매출 주문 → ④ 정비내역 입력 및 전기(Posting) → ⑤ 차량점검 보고서
```
> ④ 전기(Posting)는 되돌릴 수 없는 회계 확정 처리다. **어떤 자동화를 하든 사람이 누른다.**

**설계 원칙 (반드시 지킬 것):**
1. 새 시스템이 **현장 원본(source of truth)**, MARS는 **정기 보고처**.
2. **MARS 입력을 사람이 또 하게 만들면 프로젝트는 실패다.** 자동화를 했는데 입력처가
   하나 더 늘어나는 최악의 결과가 된다. 새 시스템이 MARS 투입용 데이터를 만들어 줘야 한다.
3. **데이터 모델은 처음부터 MARS 필드에 매핑 가능하게 설계한다.** 나중에 맞추려면 못 맞춘다.
4. MARS는 **본사 자산**이다. 구조를 바꿀 수 없고, 우리가 맞춰야 한다.
   「웹 서비스 설정」은 **보기만 하고 변경 금지.**
5. **연동 전략 = C(입력 대기열)가 본안이다.** *(2026-07-31 개정 — 본사 협의 불가 확정)*
   - **A′(OData 웹서비스) 폐기.** 발행 권한이 본사에만 있는데 협의가 불가하므로 경로가 없다.
   - **C = 유일한 확실한 수단.** 승인 불필요, 100% 구현 가능. 4주차에 만든다.
   - **D(Playwright 화면 자동화)는 선택적 보조**로만. C가 자리 잡은 뒤 검토하고,
     **전기(Posting)는 절대 자동화하지 않는다.** 상세 조건은 `docs/04` 5장.
   - 결론: **MARS 입력은 사람이 누르되, 옮겨 적는 수고를 0에 가깝게 만드는 것**이 목표다.

> 상세 연동 방안과 검증 절차는 `docs/04-MARS-연동방안.md` 참조.

### D-11. 아키텍처 확정 (2단계 산출) ⭐ (2026-07-31)

**스택**: TypeScript + Next.js 15(App Router) + PostgreSQL 16 + Drizzle ORM
+ Auth.js + Tailwind/shadcn · 배포 = Vercel + **Supabase 서울 리전**

**이식성 원칙 (D-06 이행)** — 자체 서버로 언제든 옮길 수 있게:
1. **Supabase는 순수 PostgreSQL로만 사용.** Auth·Storage·Realtime 등 고유 기능 미사용
2. **Vercel 전용 기능 미사용.** 표준 Node.js로 구동
3. **`docker-compose.yml`을 처음부터 함께 작성.** 이관 = DB 덤프 + 컨테이너 실행

**핵심 설계 결정:**
1. ⭐ **검색창은 하나.** 차량번호·전화·이름·규격·품번을 **입력 패턴으로 자동 판별**.
   정비사가 "무엇을 검색할지" 고르는 단계를 없앤다 — 현장 속도를 가장 크게 바꾸는 결정.
2. **마진·매입원가는 서버 응답에서 제외.** 화면에서 가리면 개발자도구로 보인다.
   `role='tech'` 요청에는 **값이 전송조차 되지 않게** 한다 (D-05 6번의 실제 구현).
3. **인증 = 기기 신뢰(90일) + 4자리 PIN 전환.** 매장 태블릿은 공용이고 장갑을 낀다.
   사장님 계정만 10분 미조작 시 자동 잠금 (마진이 보이므로).
4. ~~**바코드는 전역 키 감지.** 빠른 연속 입력+Enter를 스캔으로 판정 → **커서 위치 무관**.~~
   🔴 **폐기 (2026-08-04)** — 사장님: *"바코드는 써보니 생각보다 불편한 것 같으니 제외하는 방향."*
   실사용 결과가 근거다: **배운 바코드 1건 · 재고 592본 중 라벨번호 0건 · 스캔으로 만든 입출고 0건.**
   → **품목 검색으로 대체.** 매입 입고는 규격·모델·품번으로 찾아 담고, 확정은 줄마다 수량을
   넣거나 「전량 입고」로 한다. 바코드는 처음부터 필수가 아니라 빠른 입력 보조였으므로
   기능이 줄지 않는다. 관련 코드(`barcode.ts`·`barcode-lookup.ts`·`use-scanner.ts`·
   `scan-indicator.tsx`)는 삭제. `product.barcode`·`stock_item.serial`·`product_barcode` 는
   **컬럼·테이블만 남기고 읽고 쓰지 않는다** (지우는 이득보다 위험이 크다).
   ⚠️ 딸린 결과: 보관 서비스에서 「라벨 바코드로 한 본을 특정한다」는 안도 함께 사라진다.
   D-03 의 **보관번호 스티커 인쇄**로 돌아가거나 다른 방법을 정해야 한다 (미정).
5. **오프라인 쓰기는 넣지 않는다.** 조회는 캐시로 계속되지만, 입출고·견적은 온라인 필수.
   나중에 몰아서 반영하면 "두 사람이 같은 타이어를 판" 상태가 뒤늦게 드러난다.
6. **부대비용은 기본 체크 상태로 표시.** 빼는 건 쉽고, 넣는 건 잊는다 → 매출 누락 방지.
7. **다크모드 미지원 / 대시보드·차트 1차 제외.** 4주에 넣을 이유가 없다.

**데이터 모델 확정 (테이블 12개)** — `docs/09`
- `mars_customer_no` **폐기** → **`mars_contact_no`** (D-10)
- `email` 컬럼 **없음** (실데이터 0건) / `oem_tire_size` **없음** — 순정 규격은 `vehicle_spec` 별표로 (D-04 3차 개정 2026-09-03)
- ⭐ **`service_item.qty_rule`** 신설 — `per_unit`/**`per_2_units`**/`per_job`
  휠밸런스 "2개당" 함정 차단. 없으면 **매번 절반만 청구**하게 된다
- ⭐ **번호판 뒷 4자리 인덱스** — 고객은 "3456이요"라고 말한다
- **`import_issue`** 신설 — 이관 이상치 254건(규격101·중복고객139·번호판11·이름3)을
  한 화면에서 처리. 반나절 작업
- **`family_group_id`** 신설 — 같은 번호 다른 이름 72그룹을 **합치지 않고 연결만**

**상담 흐름: 누르는 횟수 7번 이내** (재방문 고객 실측 20~30초 목표)

> 상세: `docs/08-아키텍처-확정.md` · `docs/09-데이터모델-확정.md` · `docs/10-화면설계.md`

### D-12. 재고 — 타이어는 자동, 부품은 우리가 원본 ⭐ (2026-07-31)
`재고현황/` 5개 파일 분석. **`docs/11-재고데이터-분석.md`**

| 대상 | 규모 | MARS 매칭 |
|---|---|---|
| 미쉐린 타이어 | **57종 290본** (기표가 환산 1억 596만원) | ✅ **57/57 (100%)** |
| 부품 (필터 3종·브레이크패드) | **약 265종 480개** | ❌ **사실상 0%** |

**1. 타이어 재고 코드 = MARS 품번.** 엑셀만 넣으면 브랜드·규격·기표가가 자동으로 붙는다.

**2. ⭐ DOT를 이미 관리하고 계셨다 — D-02의 전제가 바뀐다.**
   `DOT(수량)` 컬럼에 `0526[8]1126[1]1826[3]` 형식으로 **107항목 296본**분이 있다. 이상 3건뿐.
   - D-02는 "DOT는 나중에 보완"이었다. 실제로는 이미 세고 계신다. **못 받으면 후퇴다.**
   - **입고 화면을 바꾼다**: 수량 한 칸 → **`DOT + 수량` 줄 입력** (엑셀이 이미 그 형태다).
     같은 DOT면 한 줄로 끝난다. 모르면 비워도 통과 (D-02 원칙 유지).
   - 🎁 **재고 노후화 경고가 공짜로 생긴다** — 2024년산 5본 / 2025년 89본 / 2026년 202본

**3. 부품은 MARS에 없다 → 우리 시스템이 원본이고, MARS로 보낼 필요도 없다.**
   - 코드 체계가 다름 (`MBA-039`, `SM188` = 공급사·제조사 품번)
   - **검색 축이 다르다: 규격이 아니라 「적용 차종」.** 차량을 조회한 상태면 자동 필터 가능
   - 바코드 없음 → **부품은 스캔이 아니라 검색으로 출고**
   - ❌ **매입가·판매가가 없다** — 부품 견적을 내려면 필요 (미결)

**4. ⚠️ MARS 「재고」 컬럼은 쓸 수 없다.** 값이 있는 것은 **전부 음수**(-2, -4, -1).
   판매만 잡히고 입고가 안 잡힌 상태다. → **재고는 우리가 100% 책임진다** (D-08 원칙 강화).

**5. ⭐ 부품 재고 수량은 신뢰할 수 없다** *(사장님 확인, 2026-07-31)*
   > "필터류 재고는 사실 맞지 않음. 확실한 것은 미쉐린 타이어 재고 뿐임."
   - **품목 마스터(265종 품번·적용차종)는 가치가 있으므로 이관한다.** 수량만 못 믿는다
   - **수량을 0으로 넣지 않는다.** 0이면 출고할 때마다 "재고 없음"이 떠서
     정비사가 시스템을 불신하게 된다
   - **틀린 숫자도 넣지 않는다.** 한 번 틀린 숫자를 보면 그다음부터 아무도 안 믿는다
   - → **`미확인` 상태로 시작한다.** 수량 자리에 숫자 대신 `미확인`을 띄우고,
     실물을 셀 때 그 자리에서 확정한다. **쓰면서 정확해지는 구조** (D-05·D-02와 같은 철학)
   - 오일필터 시트 2개(`오일필터` vs `Sheet1`) 충돌 문제는 **자동 소멸.** 어느 쪽도 안 쓴다

**6. ⭐ 미쉐린 외 브랜드는 사장님이 화면으로 직접 넣으신다** *(2026-08-01 확정)*
   > "다른 타이어도 재고가 있지만 우선 어느정도 프로그램이 돌아가는 것을 확인하고
   > 재고 수정 및 추가 기능을 활용해서 입력할 예정."
   - 즉 **1주차 엑셀 이관은 미쉐린 57종 290본으로 끝.** 나머지는 2주차 화면으로 들어온다
   - ⚠️ **함정: 상품 마스터에는 10,691건이 다 있다.** 아무 처리도 안 하면 한국·금호가
     **「재고 0본」**으로 표시된다 — 창고에 있는데 화면이 없다고 말한다. **거짓말이다**
   - → **`product.stock_tracked` 신설.** 「없다」와 「모른다」를 구분한다
     | `stock_tracked` | 재고 | 표시 |
     |---|---|---|
     | true | 4 | 🟢 4본 |
     | true | 0 | 🔴 0본 (소진) → 주문 |
     | false | 0 | ⚪ **미등록** → 창고 확인 |
   - **미등록은 접지 않는다.** 접으면 안 보이고, 안 보이면 못 판다 (소진만 접는다)
   - **미등록이어도 견적은 그대로 나온다.** 기표가·할인율은 마스터에 있다.
     재고 등록 여부가 판매를 막으면 안 된다
   - **입고 화면에 [검색] · [일괄] 탭 추가** — 스캐너 없이 규격·모델로 찾아 입고.
     일괄 모드는 확정 후 커서가 검색창으로 복귀 → 한 브랜드를 앉은자리에서 등록
   - 최초 입고 확정 시 `stock_tracked = true` → **쓰는 만큼 켜진다** (D-05·D-12 5번과 동일)

**7. ⭐ 재고는 언제든 고칠 수 있어야 한다** *(사장님 요청, 2026-08-01)*
   > "데이터가 입력된 미쉐린들도 향후에 언제나 재고 수량이나 DOT가 수정이 가능해야하며
   > 새로운 미쉐린 타이어 모델도 등록이 가능해야함."
   - 이관은 **시작점이지 확정이 아니다.** 실물과 어긋나면 그 자리에서 고친다
   - **화면 3종** — `/stock/[id]` 수량·DOT 수정, `+재고 넣기`, `/product/new` 신규 모델
   - **모든 변경은 `stock_movement`에 남긴다.** 이력이 없으면 어긋난 원인을 영원히 못 찾는다
   - 수량이 그대로여도 **`verified_at`을 갱신한다** — "확인했다"는 사실 자체가 실사다
   - 타이어(`is_serialized`)는 행이 늘고 줄고, 부품은 한 행의 수량만 바뀐다. 화면은 같다
   - 신규 등록품은 `mars_item_no`에 **`NEW-` 접두**를 붙여 MARS 이관분과 구분한다

**8. ⚠️ 말이 안 되는 DOT는 넣지 않고 비운다** *(2026-08-01)*
   - 실데이터에 **`1882`(18주 2082년)** 가 1본 있었다 — `1822`/`1826`의 오타로 보인다
   - **틀린 연식은 선입선출과 노후화 경고를 통째로 망가뜨린다.** 0을 넣지 않는 것과 같은 이유
   - → `isPlausibleDot()`: 주차 01~53 **+ 제조 15년 이내 ~ 내년**. 벗어나면 `NULL`
   - **본수는 유지하고 DOT만 비운다.** 재고가 사라지면 안 된다 (295본 그대로)
   - 화면에서 나중에 채울 수 있다 (D-02: DOT는 선택 항목)

**데이터 모델 수정 (`docs/09` 반영):**
- `tire_product` → **`product`로 일반화** (+ `item_type`, `is_serialized`, `part_no`, `fitment`, `position`)
- `stock_item`에 **`qty`** 추가 — 타이어는 1본1행(qty=1), 부품은 1행에 수량.
  **테이블을 쪼개지 않는다.** 재고 현황·출고 화면을 하나로 유지하는 게 현장에 낫다
- ⭐ **`stock_item.verified_at`** 추가 — `NULL`이면 화면에 **`미확인`** 표시
- ⭐ **`product.stock_tracked`** 추가 — `false`면 **`미등록`** 표시 (0본과 구분)
- ⭐ **재고 실사 화면**을 2주차에 넣는다. 부품 확정용이자, 4주차 타이어 실사에도 그대로 쓴다
- `fitment`에 pg_trgm 인덱스 — 부품 검색의 전부

### D-13. 작업 폴더를 OneDrive 밖으로 분리 + GitHub 비공개 저장소 ⭐ (2026-08-01)

```
C:\dev\
├── tyremore\          ← git 저장소. 코드 + docs + CLAUDE.md (여기가 원본)
└── tyremore-data\     ← 저장소 밖. 엑셀 작업본 · 개인정보
C:\Users\info\OneDrive\문서\통합자동화\   ← 사장님이 새 파일을 떨어뜨리는 곳 (자재검색·인보이스)
```

**폴더 정리 (2026-08-05, 사장님 요청):**
- 통합자동화 = `금호 상품목록\`(자재검색 xlsx — import-kumho 가 루트와 여기를 같이 훑는다)
  + `인보이스 보관\`(처리 끝난 인보이스 원본). **개인정보·보안 파일은 전부 클라우드 밖으로 뺐다.**
- tyremore-data = `mars-수출본\`(MARS 내보내기 — 고객·연락처·차량 개인정보 포함) ·
  `mars-화면자료\`(🚨 본사 API 키 찍힌 스크린샷 포함 — 구 「mars screenshots」) ·
  `스크린샷\`(mars-fill 진단 캡처, SHOT_DIR) · `인보이스견본\` · `재고현황\` · `backup\` · `chrome-mars\`

**OneDrive를 벗어난 이유:**
- 파일이 27개 → `npm install` 후 **약 40,000개.** OneDrive는 개수에 비례해 느려진다
- 기존 파일이 전부 `ReparsePoint`(Files On-Demand 자리표시자)였다 →
  빌드 도구가 대량으로 읽으면 다운로드가 밀려 **"파일 없음" 오류**가 난다
- ⚠️ `.git\index`를 OneDrive가 동기화하면 충돌 사본이 생겨 **저장소가 손상된다**
- `.next` 캐시가 저장할 때마다 다시 쓰여 동기화가 쉬지 않는다
- 경로에 한글(`문서\통합자동화`) — 일부 도구가 실패한다

**GitHub 연동 (`hisonic001/tyremore`, Private):**
- 백업이 아니라 **배포 경로다.** Vercel이 저장소를 보고 자동 배포한다(D-11) →
  `git push` 후 1~2분이면 매장 태블릿에 반영. 현장 피드백 반영 속도가 달라진다
- 자체 서버 이관(D-06)도 `git clone` + `docker compose up`으로 끝난다
- 커밋 이메일은 **`40854017+hisonic001@users.noreply.github.com`** (실제 이메일 비노출)

**🔴 개인정보 방어선은 2중이다 — 순서가 중요하다:**
1. **1차: 데이터를 저장소 밖에 둔다.** `.gitignore`는 사람이 쓰는 것이라 빠뜨릴 수 있다.
   폴더 자체가 저장소 밖이면 **실수해도 물리적으로 못 올라간다**
2. **2차: `.gitignore`** — `*.xlsx`, `*.csv`, `.env*`, `mars screenshots/`, `*MARS_*`
- **저장소는 비공개로만 운영한다. 공개 전환 금지.**
- ⚠️ 한 번 커밋된 것은 파일을 지워도 **이력에 남는다.** 커밋 전에 `git status`로 확인할 것

### D-14. 상품 표시는 화면용/MARS용/주문용 3중으로 유지한다 ⭐ (2026-08-01)

| 쓰임 | 값 | 예 |
|---|---|---|
| 화면 표시 | `display_name` ?? 자동 생성 | `PRIMACY TOUR A/S` |
| **MARS 입력** | `raw_name` + `pattern` **원본** | `Michelin 245/60 R 18 105H TL` |
| **주문 사이트 대조** | `orderName` (생성) | `245/60R18 105H TL PRIMACY TOUR A/S` |

- **원본을 절대 덮어쓰지 않는다.** MARS 입력은 원문으로 해야 한다(D-08).
- 화면만 정리하고, 필요할 때 원본을 펼쳐 본다.
- `product.display_name` — 사장님이 직접 정한 이름. 비우면 자동으로 복귀.
- **축약 사전**(`MODEL_ALIASES`): `PRIMTOURAS`→`PRIMACY TOUR A/S`, `CROSCLISUV`→`CROSSCLIMATE SUV`,
  `PILSP3`→`PILOT SPORT 3`, `PREMLTX`→`PREMIER LTX`, `PCY`→`PRIMACY` 등
- **코드 사전은 실데이터 전수 조사로 만들었다.** 사전에 없는 표기는 지우지 않고 그대로 보여준다.
  - `CPJ` = Cordon de Protection de Jante (림 보호) / `DT` = Different Tread / `GO` = 미확인
  - **화면에는 코드만 표시** (사장님 지시)
- ⚠️ 문자열 처리 순서: **규격 제거 → 붙은 것 띄우기 → 규격 제거 한 번 더**.
  순서가 틀리면 `235/45R1` 같은 잔해가 남는다.
- ⚠️ 붙은 코드는 **2개 이상 연달아 붙은 덩어리만** 나눈다. 하나씩 떼면
  `PRIMACY` 안의 `AC` 를 잘라 `PRIMAC Y` 가 된다.

### D-15. 🔴 미쉐린 주문 사이트가 MARS보다 정확하다 (2026-08-01)

`mymichelin.kr.michelingroup.com` 화면과 대조한 결과:

| | 확인 |
|---|---|
| **CAI = MARS 품번** | ✅ 확정 (`405365`, `304349` …) |
| **MARS 「단가1」 = 주문 사이트 정가** | ✅ **8/10건 정확히 일치** |
| 가격이 다른 2건 | `053990` 323,000 vs 339,000 · `065068` 299,000 vs 308,000 → **사이트가 최신** |
| **OE 마킹·XL 누락** | 🔴 MARS 내보내기에 `MO`·`*`·`GRNX`·`AC`·`EXTRA LOAD` 가 **빠져 있다** |

- 사이트 표기: `245/45 R18 100Y EXTRA LOAD TL PRIMACY 3 * MO GRNX MI`
  우리 데이터: `Michelin 245/45 R 18 100Y TL` + `PRIMACY 3` → **마킹이 통째로 없다**
- ✅ **공장도가는 VAT 미포함이 맞다** (사장님 확인 2026-08-01). 현재 ×1.1 처리 유지.
- ⚠️ **스크래핑은 두 사이트 모두 불가** — 확인 완료 (2026-08-01)
  | 사이트 | robots.txt | 약관 |
  |---|---|---|
  | blackcircles.co.kr | `allow: /` | 🔴 **제16조 2항 4호 「자동 접속 프로그램 사용」 금지** |
  | mymichelin.kr | 🔴 **`Disallow: /` (전면 차단)** | — |
  계정 정지 시 **실제 매입이 막힌다.** 화면 복사→엑셀 또는 발주하며 채우기(D-05)로 간다.

### D-16. 🔴 MARS 브랜드 코드가 틀려 있다 — GO = BFGoodrich (2026-08-01)

사장님 지적으로 확인: MARS 상품명 끝의 **`MI` = 미쉐린, `GO` = BFGoodrich**(미쉐린 자회사).

| 확인 | 결과 |
|---|---|
| `GO` 로 끝나는 상품 | 64건 중 **58건이 `brand_code='MI'`** ❌ |
| 그 모델명 | `ADVANTAGE T/A DRIVE`·`ALL-TERRAIN T/A KO3` — **전부 BFGoodrich 라인업** |
| `T/A` 가 든 상품 | 259건 중 **221건이 MI 로 잘못 분류** |

- **`brand_code` 는 건드리지 않는다.** 이관·재이관의 기준값이고 MARS와 맞춰야 한다.
- 대신 **화면 표시만 바로잡는다** (`brandHint`):
  1. 접미 `GO` → BFGoodrich  2. 접미 `MI` → 미쉐린  3. 모델에 `T/A` → BFGoodrich
  (`T/A` 는 BFGoodrich 전용 표기 — All-Terrain·Mud-Terrain·Advantage·Trail-Terrain)
- ⚠️ **브랜드 필터·VAT 설정은 `brand_code` 기준**이라 여전히 미쉐린으로 묶인다.
  BFGoodrich를 따로 다루시려면 재분류가 필요하다 — 사장님 판단 대기.

### D-17. 브랜드마다 이름 짓는 법이 다르다 — 국산은 「모델명 + 모델코드」 ⭐ (2026-08-02)

사장님 지적:
> "표기의 기준을 MARS로 잡아서 그래. MARS 관리를 미쉐린 코리아에서 사실 잘 못하는 편이거든.
>  한국이나 금호의 경우에는 `Ventus S2 AS H462`, `Solus TA31`, `Majesty Solus EDGE TA91+`
>  같이 모델명과 모델코드명이 들어가는게 순서야. 몇몇 금호타이어의 경우 뒤에 `04` 같은걸
>  그대로 붙여놨는데 그건 아마도 ply 일꺼야."

**진단이 정확했다.** 이름 규칙을 미쉐린 기준 하나로 돌리고 있어서 국산 이름이 망가졌다.
`parseTireName` 에 **브랜드 코드를 넘겨** 브랜드별로 다르게 처리한다.

| 증상 | 원인 | 조치 |
|---|---|---|
| 🔴 `iON i*cept IW01` → **`iON IW01`** | BMW 별표 규칙이 아무 데나 있는 `*` 를 잡았다. 한국 모델명에 별이 들어간다 (`i*cept`·`i*Pike`) | `*` 가 **홀로 선 토큰**일 때만 BMW로 본다 |
| `Vantra LT RA18` → `Vantra RA18` | `LT` 를 배지로 떼면서 모델명에서도 지웠다 | 모델명 중간의 `LT` 는 **남긴다** (맨 앞은 규격 잔해라 뺀다) |
| `Solus TA31 98V **04**` 의 `04` 가 이름에 남음 | 겹수(ply)를 모델명으로 봤다 | **금호만** 끝의 `04·06·08·10` 을 `4PLY` 배지로 뺀다 |
| `Majesty X Solus **흡음**` | 금호는 흡음재를 **한글로** 적는다 | `흡음` 을 흡음재 배지로 인식 |
| `8P` 배지에 숫자가 없음 | 코드가 그냥 `PLY` 였다 | `8PLY`·`12PLY` 로 숫자를 붙인다 (`12PR` 도 인식) |
| `185R14 KC53 6 Portran` 에 규격이 남음 | 편평비 없는 밴 규격(`185R14`·`145R13C`)을 못 걷었다 | 규격 제거 규칙 추가 |
| `Winter i*Pike LV(RW15) **RW15**` | MARS가 모델코드를 두 번 적어 놓았다 | 연달아 같은 토큰이면 하나로 줄인다 |

> 🔴 **겹수 분리는 금호에만 적용한다.** 다른 브랜드의 끝 숫자는 **세대 번호**다 —
> `CROSSCLIMATE 2` · `PILOT SPORT 4 S`. 떼면 서로 다른 타이어가 같은 이름으로 나온다.
> 홀수도 건드리지 않는다 (`Majesty 9 Solus`).

**검증**: 타이어 10,318건 전수 — 모델명이 비거나 규격이 남은 것 **3건**
(`195R15N12`·`195R15R`·`27X8.50R14 834` — 셋 다 MARS 원문 자체가 규격 형식이 아니다).

**남은 원칙**: 자동 판정이 못 잡는 것은 **세부사항 고치기**(D-16 이전 커밋)로 손보면
`attrs_override` 에 남아 재이관해도 유지된다. 규칙을 고치면 손대지 않은 것이 저절로 좋아진다.

### D-18. 간편결제는 카드와 다른 돈이다 — 카드 일마감은 「남은 돈」으로 센다 ⭐ (2026-08-29)

사장님 제보 4건(카드 화면·판매 등록·올리기)에서 나왔다.

**① 간편결제 신설.** QR·네이버페이·카카오페이·토스페이. 단추는 **하나**다 — 어느 페이인지는
토스 포스 「매입사」 칸에 이미 적혀 온다 (실측 2026-08-28: 토스페이·현대·삼성).
🔴 **여신금융협회 승인내역에는 안 잡힌다.** 그래서 앱에 「카드」로 적으면 매달 차이가 났다.
정본 = `src/lib/payments.ts` SPLITTABLE. DB 제약은 `scripts/add-easypay.ts` (코드 배포 **전에** 실행).
MARS 에는 카드로 보낸다 (매입사가 카드사로 찍히는 건이 대부분).

**② 일마감 자국은 「짝 + 금액」이다.** 자국 한 줄 = 「이 POS 건의 얼마가 이 판매에 갔는지」.
POS·앱 양쪽이 **붙은 돈 / 남은 돈**을 갖는다. 이 하나로 사장님이 꼽은 예외가 전부 풀린다:
카드 두 장으로 나눠 긁기(POS 여럿 = 판매 하나) · 한 번 긁어 두 건 치르기 · 미리 받고 나중에 더 받기.
- 자동 대조 3단: ① 1:1 정확 ② POS 여러 건 합 = 앱 1건 ③ POS 1건 = 앱 여러 건 합.
  🔴 **답이 둘 이상이면 안 붙인다.** 미배정이 12건 넘으면 ②③을 안 돌린다 (조합 폭발).
  🔴 **수단이 다르면 자동으로 안 붙인다** — 「간편결제로 고치기」 단추로만.
- 다른 날 후보는 앞뒤 1일 → **7일**. 단, 다른 날 후보는 **금액이 딱 맞을 때만** 띄운다
  (POS 자료가 없는 지난 날 판매 하나가 모든 POS 건의 후보로 뜨던 것 — 8/28 실측).
- 「선결제 — 판매는 나중에」 사유 신설. 판매가 생길 때까지 일마감 화면 맨 위에 남는다.
- 🔴 낱말 상수는 **`src/lib/pos-vocab.ts`** (DB 안 씀) — 화면이 `pos-close.ts` 에서 가져가면
  서버 코드가 브라우저 묶음에 딸려간다.

**③ 카드 맞추기(달)는 3열이다.** 여신협회 · 토스POS · 앱을 나란히. 전엔 POS 가 「여신 없는 날만
대신 채우는 폴백」이라 두 자료를 견줄 수가 없었다. **차이는 POS − 앱**으로 잰다(실제로 긁힌 돈이고
간편결제가 들어 있다). POS 가 없는 날만 여신 − 앱. 비교 가능 판정은 `CardDayRow.base` 하나로 —
전엔 `card-recon` 과 화면이 따로 재서 집계와 회색 처리가 어긋났다.

**④ 우리카드는 형식이 둘이다.** 「승인 상세내역」(승인 기준 — 연·시각·승인번호·사업자번호·부가세)과
「이용대금 상세내역」(청구서 — 이용일자가 MM.DD 뿐). 새로 「승인 상세내역」을 읽는다.
🔴 **같은 기간을 둘 다 올리면 같은 지출이 두 번 잡힌다** — 청구서엔 승인번호가 없어 중복 방지 키의
모양이 아예 다르다. 올리기 미리보기가 `fin_upload.raw_text` 로 겹치는 배치를 찾아 붉게 알린다
(막지는 않는다 — 되돌리기 길을 같이 준다).

### D-19. 콘티넨탈은 자재번호(Material)가 열쇠다 ⭐ (2026-08-29)

사장님 지시: *"콘티넨탈 품목을 재정리해야함. 운영 규격 표를 기준으로 삼고
**material 번호들을 기준으로 삼아서 중복이 없도록** 하면 될듯."*

**기준 자료 두 개가 한 벌이다** (`통합자동화\콘티넨탈 상품목록\`)
| | 운영 규격_2026 | Winter 주문서_2026 |
|---|---|---|
| 줄 | 560 (CONTINENTAL 529 + **GENERAL 31**) | 176 |
| 열쇠 | `Material` 11자리 | `Article #` 11자리 |
| 머리글 | 1줄 | 🔴 **6번째 줄** (위에 안내 5줄) |
| 이름 | `Description` | `Description` + **`Marketing Line`**(정식 모델명) |
| 가격 | 기표가 + 공장도가(=기표가×1.1, 560줄 전부 일치) | `2026 List Price` |

- 🔴 **두 파일의 자재번호가 하나도 안 겹친다**(실측) → 합쳐 「2026 목록 737건」(사장님 확인분 1 포함)
- 🔴 **두 파일 모두 가격은 부가세 미포함**이다. `list_price = round(excl × 1.1)`
- 🔴 **겨울은 운영 규격표에 아예 없다.** 표만 보고 접었으면 재고에 있는 겨울 타이어가 통째로 사라졌다
  (재고 22품목 75본의 대부분이 WinterContact 다). 겨울 주문서를 받아 함께 다뤘다.

**우리 품번이 이미 `CO`/`GN` + 자재번호였다** (`CO03201650000`) — 960/961. 금호 때의
「자재코드가 어디에도 없다」 문제가 없어 훨씬 수월했다. 그래도 **정본은 `supplier_item_code`** 다
(D-08 원문 보존 — `mars_item_no` 는 안 고친다). `continental_material` 표를 새로 뒀다.

**결과**: 잇기·갱신 692 · 새로 만듦 45 · 접기 266 · 합치기 2 · 이름 172가지 ·
재고 75본 불변 · 부가세 어긋남 0 · 재고 있는데 접힌 것 0.

**🔴 콘티넨탈 이름 규칙은 금호와 반대다** (`src/lib/conti-name.ts`)
- 금호의 **겹수 분리(끝 숫자)를 절대 끌어오면 안 된다.** 콘티넨탈은 모델명이 숫자로 끝난다
  (`EcoContact 6`·`SportContact 7`·`UltraContact UC6`). 겹수는 `8PR` 처럼 명시적일 때만 본다.
- `#`·`★`·`(+)` 는 **다른 물건**을 가르는 표시다. 중복 판정에서 기호를 지우면
  `DWS06 PLUS` 와 `DWS06 PLUS #` 가 같은 것으로 잡힌다 (금호에서 겹수를 안 넣어 데인 것과 같은 자리).
- 목록의 오타·흔들림: `M0`(숫자 영) → `MO`(벤츠) · `Conti ProContact` → `ContiProContact` ·
  `*MO` 처럼 별표가 붙어 온다 · 축약형 `VANCAP`·`CEC 5`·`CCRX`·`ASC 2` 는 사전으로 편다.
- 접두 `LT`·`P`·`HL`·`ML`, 밴 규격 `185R14C`(편평비 없음), 괄호 하중 `(103Y)` 를 다 처리한다.
  736줄 전수 **규격 실패 0 · 이름 빈 것 0**.

**⭐ 제네럴타이어(GENERAL)는 콘티넨탈이 아니다** (사장님 지적 2026-08-29).
`brand_code = 'GN'`(제네럴타이어) 로 따로 간다 — 보임 31 · 접힘 4(ALTIMAX RT43).
접기 사유 문구도 브랜드를 따라간다. ⚠️ **콘티넨탈 인보이스에 제네럴이 섞여 온다** (`invoice.ts:33`).

**⭐ 235/55R19 CrossContact RX 는 두 가지가 실제로 있다** (사장님 확인)
`15581850000`(2025년산·하중 101·멕시코·OE 카니발) vs `03595710000`(2026년산·하중 **105**·태국·
OE 24년형 카니발). **하중지수가 다르면 다른 타이어다 — 합치지 않는다.** 목록에 없는 쪽은
사장님이 읽어 주신 값으로 자재 마스터에 넣어 뒀다 (`conti-sheet.ts` OWNER_EXTRA).

**재고는 성역** — 재고·판매가 있는데 목록에 없는 것은 자동으로 접지도 합치지도 않는다.
후보와 함께 뽑아 사장님이 고르신 뒤 `--merge` 로만 (이번엔 3건 → 2건 합침, 1건 그대로).

**곁다리 수리**: `product-merge.ts` 가 흡수 품번을 **무엇이든 「금호」 사전에** 넣고 있었다 —
접두로 거래처를 가르게 고쳤다 (`KM…`=금호 · `CO/GN…`=콘티넨탈 · 모르는 것은 안 넣는다).

### D-20. 예약금은 그때그때 다르다 — 덜 받은 판매는 「외상 + 받은 몫」, 다 받으면 앱이 보통 결제로 되돌린다 ⭐ (2026-09-10)

- 사장님: "안 받고 진행도 하고 받기도 하고, 결제 방식·금액은 정해진 것이 없음." 정액·비율 가정 금지.
- 저장 모양은 기존 규약 그대로 — `payment_method='외상'` + `receivable_payment`. 새 칸을 만들지 않는다
  (`quote.paid_amount` 는 늘 총액과 같은 죽은 칸 — 읽는 코드 0).
- 등록: 예약 켜면 수단이 비고(「예약금 안 받음」 기본), 받았으면 수단을 눌러 받은 만큼만. 빈 칸 = 0원.
- 이미 저장된 예약: 정비 내역 카드 ⚠「받은 돈 고치기」(`reservation-fix.tsx` → `restateReservationPaid`).
- 완납되면 `normalizeSettledReservation` 이 수금 줄을 `quote_payment`(받은 날 보존)로 옮기고 '혼합' —
  카드 일마감·MARS 가 예외 없이 맞물린다. 개인계좌가 섞이면 외상으로 남긴다. 정본 `src/lib/reservation-pay.ts`.
- 🔴 전엔 등록 때 예약+카드만 누르고 금액을 비우면 **전액 카드**로 굳었다(실측 예약중 8건 전부). 시공 후
  MARS 가 외상을 막고, 안내대로 결제를 바꾸면 먼저 받은 예약금이 카드 일마감에서 사라졌다 — 그 맞물림을 끊은 것.

### D-21. 돈관리 탭은 「할 일 / 장부 / 자료」 3묶음 + 소탭 — 화면은 안 옮긴다 (2026-09-10)

- 사장님 선택: 「탭만 3개로 묶기 — 지금 구조는 그대로 두고 탭 이름만 정리」. 정본 `src/components/fin/tabs.tsx`.
  (처음) 할 일 = 현황·입금 정리·계산서·지출 / 장부 = 거래처·미지급·받을 돈(/receivables)·추적 / 자료 = 올리기·올린 자료·카드 마감.
  **5단계(D-39, 09-13)부터**: 할 일 = 현황·이번 주 정리·최근 한 일 / 장부 = 손익·거래처·입금 정리·지출·계산서·미지급·받을 돈 / 자료 그대로. 추적 없음.
- 「올린 자료」 `/finance/files` — 정본 `src/lib/upload-ledger.ts`: 언제 무엇을 올렸나 · 비었나(`upload-coverage`) ·
  배치별 「정리 안 된 N줄」(`upload_id` 로 셈) · 검색은 파일로도, **줄의 적요·금액으로도**(어느 파일에서 왔나).
- 정합성 검사는 (처음) 배너가 지문(`audit_run.fingerprint`)이 바뀌면 열 때 다시 찍었다(`freshAuditRun`) →
  **5단계에 뺐다**: GET 중 쓰기이고 A1 이 60+ 질의라. 이제 07:30 cron + 장부 「지금 다시 검사」 단추만 쓰고, 마감 체크리스트 한 줄은 읽기만(D-39).

### D-22. 돈관리 첫 화면 = 「오늘 · 이번 주 정리 · 이번 달 돈」 세 칸 — 개편 1단계 (2026-09-11)

- 사장님 결정(질문 5라운드, 설계서 `~/.claude/plans/…distributed-orbit.md`): 리듬 = 매일 저녁 5분 + 주 1회 정리 ·
  PC 주, 폰은 보기만 · 첫 화면 A2(세 칸 나란히, "직관적이고 한눈에") · 한 줄 = 무엇·숫자·단추 하나, ✅/🟡/⚪ 색.
- 정본: `today-brief.ts`(오늘 4줄) · `weekly-steps.ts`(6단계 + 지난달 마감, 마감 체크와 같은 함수) ·
  `invoice-deadline.ts`(전월 계산서 10일 경고 — 판정은 `settleTaxCandidates` 재사용, 대상 = 전월 외상 있는 모든 거래처,
  본사청구 제외, 건너뛰기 `app_setting.invoice_deadline_skip`) · `receivable-total.ts`(못 받은 돈, 정비 내역 배너와 공유) ·
  `payableTotal()`(줄 돈). 첫 화면에 인라인 SQL 없음.
- 손익 구성·근거(「어디서 온 숫자?」)·요약·내역·월 마감은 **장부 첫 화면 `/finance/ledger`** 로. 첫 화면은 숫자만.
- 정합성 A1 은 「오늘 · 안 들어온 이체」가 대신. 배너·인박스는 3단계까지 접힌 채 두었다가 **5단계(D-39)에 없앴다** —
  A2~A4 는 장부 마감 체크리스트 한 줄(`month-close.auditCheck`), 상대별 묶음은 흐름·거래처 원장.
- 같은 날 받은 예약금·수금은 정비 내역에서 **별도 카드 없이** 판매 카드가 「예약금 N 받음 · 잔금 M」으로 보여 준다
  (`sale-history.ts sameDayAsSale`, 사장님 09-11).
- 다음: 2단계 「최근 한 일」+용어 → 3단계 한 줄 흐름 → 4단계 자동 맞춤·규칙 학습 → 5단계 정리.

### D-23. 계산서 화면 = 「상대별 한 줄 → 펼치면 장 카드(누구·돈)」, 월정산은 8/25 기준 시작 잔액 (2026-09-11)

- 사장님 결정(질문 4라운드): 한 장 = 한 카드 두 칸(누구 → 돈) · 월정산 거래처는 거래처 한 줄(계산서 합 vs 준 돈 vs 남은 돈, 단추 없음 —
  남은 돈 0이면 앱이 끝) · 매입·매출 한 화면 · 사람 단추는 [맞추기][경비][나중에][안 봄] 넷 · 대행사(카랑·오픈링크·레드캡)는 장 단위 +
  「누구 대신 끊음 · 어느 달 청구 · 차이」 사슬 · 앱 입고 합 한 칸 비교.
- **시작 잔액 = 세무사 원장(2026-01-01~08-24) 잔액, 기준일 2026-08-25** — 미쉐린 36,173,936(「1,982,000 차이」= 로열티 6회 자동이체) ·
  스칼릿 5,281,980 · 금호 2,997,316 · 콘티 1,413,026 · 프로디테일 45,160 · 딜러타이어 0(페이머니 선급 17,000) · 강남세차장 14,322,000(앱 추정,
  적요 「최선종(강남세…)」) · 위즈오토 0 · 쌍성 0 · 맥스런 0. `tax_party_rule.baseline_*`, 화면에서 고침(`setTaxBaseline`).
- 정본 `src/lib/tax-book.ts`(`taxBook(ym)` 하나 — 판정은 taxCashData·taxReconV2·sureTaxPicksFrom·monthlyRemain·agencyChainOf 재사용),
  계약 `tax-book-types.ts`, 화면 `finance/tax/tax-book-ui.tsx`. 옛 money-view·tax-ui 삭제, `link-parts.tsx` 부품만 남김(입금 화면 공용).
- `taxOpenCounts` 는 월정산 거래처를 **거래처 1건**으로 셈. 기준일 전 미대조(222장)는 할 일에서 빠지고 원장에서 봄.
- 한 화면 질의 ~130개(3.8초) — 다음 손질 후보: taxCashData 월정산 루프와 monthlyRemain 의 partyStrictNames 중복.

### D-24. 「최근 한 일」 한 곳 + ERP 용어 (2026-09-12, 돈관리 개편 2단계)

- 사장님 결정 14·15: 되돌리기는 **/finance/activity 한 곳**(최근 30일 + 달 고르기, 내가/앱이 칩), 마감 뒤 고치면 기록에 남고
  마감 때 이익과 지금 이익의 차이를 띠로(`closedDelta`). 용어는 "한국 ERP 기준으로 명료하게" — 「반제」는 어려우니 「지급 확인」.
- 표 `fin_activity`(`scripts/add-fin-activity.ts`, 실행 완료) — 되돌린 줄은 지우지 않고 `undone_at` 만. 일괄은 한 줄 n건 + `undo_args.items`.
  기록은 **코어 함수 안**(recon-core·deposit-core·pos-close·expense-core…) 한 줄 `logActivity()`(throw 안 함, 커밋 뒤 db 로) —
  화면·연간 실행기가 같은 코어를 쓰므로 한 곳. 코어에 넣었으면 액션에는 안 넣는다(이중 기록 금지).
- 되돌리기 `undoActivity`(`fin-activity-actions.ts`)는 **기존 undo 함수만** 라우팅(`fin-activity-types.ts` 상단 표). 「되돌리기」 줄은
  undo 함수 안에서 남기고 undoActivity 는 안 남긴다(두 줄 방지). 각 화면의 접힌 되돌리기 표 10곳은 「최근 한 일에서 되돌리기 →」 링크로.
- 용어 정본 `src/lib/fin-words.ts`(`W`·`statusWord`·`verbWord`·`autoReconLabel`) — 화면 글자는 여기서만. 잇기·맞추기·붙이기→**대조**, 자국→대조 내역,
  확정→대조 완료, 미대조→미대조(그대로), 제안→대조 후보, 이을 것 없음→대조 제외, 도장→**지급 확인**, 예치금→선급금, 나중에·대기→보류,
  무시·안 봄→제외, 상쇄→상계, 못 받은 돈→미수금, 줄 돈→미지급금, 번 돈·쓴 돈·남은 돈→**매출·비용·이익**. 🔴 DB 값('확정'·'제안'·'무시'·'대기')은 그대로.
  ⚠️ 처음엔 「대사」였다 — 사장님이 하루 써 보고 "여기저기 쓰인 게 어색함"(09-12) → 후보 셋(맞추기/대조/확인)을 화면 줄로 비교해 드리고 **「대조」** 확정(`b85b34a`).
  `fin_activity.verb` DB 값은 여전히 '대사'이므로 화면에는 `verbWord()` 로 찍는다. MARS 의 대사(對査)·`mars_run.kind='대사'` 는 다른 말이라 손대지 않는다.
- 지출 「분류」·POS 「미리 받은 돈」(선수금)·월정산 「잔액」은 대조가 아니라 그대로. 8월 마감 이익 −301만 vs 지금 −1,035만(마감 뒤 자료 추가) —
  첫 after_close 줄이 생기면 띠에 그대로 보인다.

### D-25. 배터리 단가표는 상수표 한 곳, 원가는 스크립트로만 (2026-09-12)

사장님이 「2026-09-01 싸군배터리 인상 단가표」 사진을 주며 "앱에서 볼 수 있고 원가도 반영되게".

- **정본 `src/lib/battery-price-list.ts`** — 사진을 옮긴 상수표(브랜드·계열·품명·공급가 126종). **화면과 스크립트가 이 한 표를 같이 본다** — 두 벌이 되면 곧 어긋난다.
  다음 인상 때는 **이 파일의 값과 `PRICE_LIST_DATE` 만 고치고 스크립트를 다시 돌린다.** 화면은 손댈 필요 없다.
- 화면 `/stock/battery`(정본 `battery-price.ts` 질의 2번) — 재고 → 부품 「배터리」 묶음 머리의 「단가표 →」로 들어간다.
  줄의 **순서·품명은 상수표**, **값은 DB 의 지금 값**. 사 오는 값은 `hasPerm("cost")` 인 사람에게만 서버에서 내려보낸다.
- 🔴 **사 오는 값은 화면에서 못 고친다** — 단가표가 정본이라 화면에서 고치면 표와 앱이 갈라진다. 파는 값(기표가)만 줄마다 고친다(`setListPrice` 재사용).
- 원가 반영 `scripts/battery-price-20260901.ts`(`--dry` 로 집계만) — `part_no` 로 짝, 안 붙는 둘은 상수표에 `productId` 고정.
  **짝이 없으면 만들지 않고 보고만** 한다(품번만 있는 유령 품목이 생기면 검색에 안 걸린다). 사진에서 안 읽힌 칸(`price: null`)은 건너뛴다.
  「전 값 → 새 값」은 새 표 없이 `app_setting` 한 행(`battery_price_20260901`)에 — 화면이 읽어 「전 78,900」을 보여 준다. 사람이 읽을 기록은 `docs/배터리단가-20260901.md`.
- 🔴 **이미 판 줄의 `quote_item.purchase_cost` 는 소급하지 않는다** — 그 배터리는 인상 전 값에 사 둔 재고이고, 판매 시점 원가는 일부러 동결한 값이다(`sale.ts:421`).
- 배터리 판매는 전부 `line_type='use'`(「쓴 부품 담기」)라 **기표가를 적어도 0원으로 들어가던 것**을 고쳤다(`sale/client.tsx` `unitPrice: p.listPrice ?? 0`).
  기표가가 없는 필터·패드는 그대로 0원 — 「기본 0원」(2026-08-24) 결정은 그대로다.
- 첫 반영 결과(09-12): 126종 중 119종 갱신, 113↑ 4↓ 2= 평균 +8.9%, 원가가 비어 있던 `HK80DL`·`HK100DL` 채움. 손이 필요한 것: `GB250R` 짝 없음(앱엔 `GB250L`),
  사진에서 안 읽힌 4종(로케트 `DIN54459`·`DIN55457`·`DIN56318`, 한국 `HK54DL`), 중복 의심 2쌍(`로케트 GB55065`↔`DIN55065 R형`, `델코 DF65-900 85Ah`↔`DF65-900`).

### D-26. 재고가 모자라도 판매는 통과한다 — 대신 **소급**과 **감시기**를 둔다 ⭐ (2026-09-09~10)

재고 조사에서 나왔다. 전산 재고가 0일 때 판 판매(예약 시공완료 포함)는 **차감이 조용히 빠졌고**,
나중에 입고돼도 소급이 없어 **앱 재고가 실물보다 부풀었다**(실측 26건).

- 🔴 **판매를 막지 않는다.** 막으면 손님을 세워둔 채 시공이 멈춘다 — 현장이 시스템을 버린다.
  대신 빠진 차감을 **나중에 따라잡는다.**
- **정본 `src/lib/stock-catchup.ts` 의 `catchUpShortSales` 하나.**
  🔴 **마지막 실사 이후 판매만** 소급한다 — 실사는 실물을 센 것이라 그 전 미차감은 이미
  반영돼 있다. 그 전까지 소급하면 **이중 차감**이다. 멱등이라 다시 돌리면 0건.
  실행 `npx tsx --env-file=.env.local scripts/fix-stock-shortfall.ts` (보기만) / `--fix` (차감).
- **감시기 `scripts/stock-integrity-check.ts`** — "재고가 조금씩 달라지는 느낌"이면 이것부터.
  판정은 정본 `src/lib/stock-integrity.ts` 에 있고 **「자료 사진」(ops-snapshot)도 같은 정본**을
  쓴다. 스크립트는 보여주기만 한다 — 두 벌이 되면 화면과 보고서가 다른 말을 한다.
- D-12 7번의 **「모든 변경은 `stock_movement` 에 남긴다」는 예외 없다.** movement 없이 수량을
  바꾸는 코드를 새로 만들지 않는다. 이력이 없으면 어긋난 원인을 영영 못 찾는다.

### D-27. 고객 섞임 방지 — 더미 전화·자리표시 이름은 **매칭 열쇠가 아니다** ⭐ (2026-09-09)

서로 다른 손님이 한 고객 행에 섞이는 사고가 났다. 원인은 **등록 시점의 매칭**이다.

- 🔴 `010-1234-5678` 같은 **더미 전화**와 「고객」·「관광객」 같은 **자리표시 이름**으로는
  기존 행에 붙이지 않는다. 붙이면 남남이 한 사람이 된다. 정본 `src/lib/normalize.ts`.
- **원문은 절대 바꾸지 않는다** — 정규화 값은 검색용 사본으로 따로 둔다.
  `고태환[한진택배]` 는 사장님이 손님을 기억하는 방식이다 (D-10 3번).
- **감시기 `scripts/customer-integrity-check.ts`** — 전부 0건이 정상. 점검 3가지:
  ① 판매의 고객 ≠ 그 판매에 붙은 차량의 소유주(거래처 차고 제외) ② 자리표시 이름 행에 차량 2대 이상
  ③ 더미 전화 행에 이관 뒤 새 차량·새 판매가 붙음.
- 🔴 **이미 섞인 것을 푸는 일은 Claude 가 임의로 하지 않는다** — 사장님이 직접 실행하신다.
- 🔴 **과거 테스트 데이터의 고객·번호판 겹침은 합치지 않는다** (사장님 2026-08-08).
  의미 없는 옛 자료이고, 본질은 **등록 시점 방지**다. 합치기를 다시 제안하지 않는다.

### D-28. 뒷마진(제조사 장려금)은 **월별 「층」**으로 따로 — 모르면 0원 ⭐ (2026-09-09)

앞마진(건별 스냅샷, `margin-def.ts`) + 뒷마진(월별 층) = 진짜 마진. 정본 **`src/lib/rebate.ts`**
(표는 `scripts/add-rebate.ts` 로 만든 `promo` · `rebate_entry`).

- **추정(미확정)**: 활성 프로모션의 실적으로 자동 계산. 🔴 **문턱 미달·자격 미확인·달성 불확실은
  0원**(콘티 RSP 등) — **기대치를 마진으로 착각하지 않게** 하는 것이 이 층의 존재 이유다.
- **확정**: 크레딧 메모·매출할인이 실제로 도착하면 `rebate_entry` 에 수기 등록(실적월 귀속).
- 🔴 **`settle_type='에누리'`는 어느 층에도 넣지 않는다** — 세금계산서 공급가액이 이미 깎여
  와서 매입 원가에 반영돼 있다. 넣으면 **이중 계상**이다.
- 기표가는 매입 줄의 `unit_list_price`(VAT 제외) — 역산이 아니라 **전표 원본 값**이다.
- 새 행사는 **안내문을 받으면** 그때 추가한다. 조건을 짐작해서 미리 넣지 않는다.

### D-29. 본사청구 판매 = **손님 판매 + `claim_party`** ⭐ (2026-09-10)

미쉐린 데미지 쿠폰·OE AS 는 **손님 차에 시공하지만 돈은 본사가 준다.**

- 🔴 **`supplier_name` 을 쓰지 않는다.** 쓰면 MARS 가 「해당없음」으로 굳어 **소매 등록이 막힌다**
  (D-08 의 MARS 전송이 통째로 어긋난다). 그래서 칸을 따로 뒀다 — `quote.claim_party`.
- **받을 돈으로는 거래처와 똑같이 묶인다.** 정본 **`src/lib/receivable-key.ts`** 의
  `receivableKeySql` 하나. 전에는 같은 CASE 문이 `receivable-book`·`fin-deposits`·
  `settlement-apply` 세 곳에 복붙돼 있었다 — 한 곳만 빠뜨리면 **장부엔 보이는데 수금은 안 되는**
  식으로 갈라진다. 외상 대상이 늘면 **여기만** 고친다.
- 🔴 **비회원은 건별로 하나의 대상**(`'W:'||id`)이다. 이름으로 묶으면 동명이인의 외상이 한
  덩어리가 되어 **남의 것을 대신 털게 된다.**

### D-30. 🚨 앱이 전면 마비되면 — 좀비 질의를 끊는다 (2026-08-07 풀러 마비 사건)

**증상은 항상 같다: 모든 화면이 「계속 로딩중」.** 배포 실패가 아니라 **DB 자리가 꽉 찬 것**이다.

- **연결 풀은 `max: 3`** (`src/db/index.ts:25`). 적게 잡은 것이 맞다 — Supabase 세션 풀러는
  **전체 15자리**뿐이라 `max: 10` 이던 시절엔 둘만 겹쳐도 홈이 500으로 죽었다(2026-08-05).
  화면 하나가 동시에 날리는 질의는 2~4개라 3으로 충분하다.
- 🔴 **그래서 좀비 질의 3개면 매장이 통째로 멈춘다.** 끝나지 않는 질의(`active`·`ClientRead`)가
  자리를 깔고 앉으면 새 요청이 하나도 못 들어온다.

**푸는 법 — `npx tsx scripts/db-unstick.ts`** (npm 별칭 없음). 세 가지를 한다:
① 2분 넘게 걸린 질의를 끊는다(읽기라 자료는 안전) ② `statement_timeout 60s` ·
`idle_in_transaction_session_timeout 120s` 를 **DB 역할에 박는다** → 재발해도 1분 안에 자가 치유
③ 트랜잭션 풀러(6543)가 뚫렸는지 확인(막혀 있으면 30초 뒤 한 번 더).

- 🔴 **이 스크립트는 사장님이 `!` 로 직접 실행하신다.** 앱이 멈춘 상황이라 사람이 눌러야 빠르다.
- 🔴 **`statement_timeout` 은 앱 코드에 없다** — DB 역할 설정이라 **DB 를 새로 만들거나 역할이
  초기화되면 사라진다.** 그때는 db-unstick 을 한 번 돌려 다시 박아야 한다.
- 소켓은 **5분마다 갈아 끼운다**(`max_lifetime`). 풀러가 이미 닫은 낡은 소켓에 질의를 쓰다
  죽거나 어중간하게 멈춰 좀비가 되던 것이다(2026-08-11 무한로딩 2차).

**포트 — 아직 전환하지 않았다.** 지금 `DATABASE_URL` 은 **5432(세션 풀러)** 다.
6543(트랜잭션 풀러) 대비는 `prepare: false` 로 해 뒀지만(이 줄 없이 포트만 바꾸면 **모든 질의가
죽는다**), 🔴 **매장 PC 의 `scripts/mars-agent.ts` 는 5432 전용**이다 — 6543 에서는 잠금이
유지되지 않는다. 전면 전환은 이것부터 풀어야 한다.

> 병렬 갈래를 돌릴 때의 DB 수칙은 뒤의 **「🔴 DB — 한 번에 하나」** 를 볼 것. 같은 사건이 뿌리다.

### D-31. 차대번호는 **해독할 수 없다** — 대신 차량 카드를 사진으로 **읽는다** ⭐ (2026-09-04~08)

별도 `C:\dev\carinfo` 실험실은 **폐기**하고(사장님 2026-09-04) 앱 안 **`/carinfo`** 로 들어왔다
— 아직 등록 안 된 차를 조회하는 화면이다(`src/app/carinfo/`).

🔴 **차대번호(VIN) 해독은 불가능하다. 다시 조사하지 않는다.**
- **4~9자리의 뜻은 제작사가 교통안전공단에 따로 제출하는 비공개 자료다.** 공개 경로가 없다.
- NHTSA vPIC 를 시험했더니 **우리 내수 차는 모델이 전부 `null`** 이었고, 르노 SM5 를
  **「NISSAN 1984년」이라고 자신 있게 틀렸다.** 틀린 답이 오는 것이 없는 것보다 나쁘다.
- 읽을 수 있는 것은 셋뿐(`src/lib/vin.ts`): **1~3자리 제조사·생산국 · 10번째 자리 연식 ·
  뒤 6자리 일련번호.** 차종은 **제안만** 하고 굳히지 않는다 (D-04 의 정신).

**그래서 하는 일은 「해독」이 아니라 「읽은 것 검사」다** (`src/lib/vin-photo-core.ts`) —
B필러 차량 카드·자동차등록증에 **적혀 있는 차명을 그대로 읽는다**(사장님 지적).

**판매 등록의 사진 흐름** (`src/app/sale/photo-assist.tsx`) — 사장님 원문:
> "차량번호 사진 + B필러 사진(혹은 차량등록증) + 주행거리, **세 장의 사진으로 고객 조회와
> 새로운 고객 등록이 바로** 가능하게."

- 🔴 **2026-09-06 개정: 한 장씩 찍는 방식은 폐기했다** ("한 장씩 순서대로 찍는 것은 불편함").
  **앨범에서 여러 장을 한꺼번에** 고른다 — `MAX_PHOTOS = 4`.
- 붙는 곳은 `sale/customer-pick.tsx`(개인 · 거래처 차량 둘 다).
- 🔴 **개인정보 경계가 화면마다 다르다**: `/carinfo` 는 차량번호·이름을 **안 읽는다.**
  `/sale` 판매 등록에서만 번호판·주행거리·소유자 이름을 읽고, **주소·전화는 어디서도 안 읽는다.**
  사진은 읽고 나면 **즉시 지운다**(`vin_scan.image = NULL`).
- 사진 읽기 실행기는 **매장 PC** 에서 돈다(`scripts/vin-photo.ts`). 어긋남 점검기
  `scripts/check-vin.ts` 는 **고치지 않고 목록만 낸다.**

**🔴 매장 PC 대리인의 판 번호 — `AGENT_PROTOCOL`** (`src/lib/agent-version.ts`, **현재 7**)
- **대리인이 처리하는 주문 종류를 늘리거나 바꾸면 이 번호를 반드시 올린다.**
  종류는 `KNOWN_JOB_KINDS = ["초안","스캔","발행사진","차량사진"]` — 모르는 종류는 흘려보내지
  않고 실패시킨다.
- 🔴 **대리인 스크립트에 `git pull` 이 없다**(저장소 전체 0건). 번호를 올리면 **사장님이 매장 PC
  에서 직접 `git pull` 하고 대리인 창을 닫았다 다시 켜셔야** 한다. 앱이 안내문을 띄운다.

### D-32. 새 화면은 `components/ui` 부품으로 짓는다 — 토스풍 디자인 (2026-08-25 사장님 승인)

정본은 **`src/app/globals.css` 의 `@theme` 블록**이다. 🔴 **`tailwind.config` 파일은 없다**
(Tailwind v4라 색이 CSS 안에 있다) — 색을 찾을 때 헤매지 말 것.

| | 값 | 🔴 쓰는 법 |
|---|---|---|
| 초록(간판색) | `--color-brand-500: #009944` | **아이콘·링·큰 글자 전용. 작은 흰 글자 금지** (대비 3.7:1) |
| **주 버튼 정본** | `--color-brand-600: #007a36` | 흰 글자 5.4:1 AA — **버튼은 이쪽이다** |
| 노랑 | `--color-accent-400: #ffe500` | **검정 글자 전용.** 흰 글자를 올리지 말 것 |
| 모서리 | `--radius-card: 1rem` · `--radius-control: 0.75rem` | |

- **공용 부품 12개** (`src/components/ui/`): `amount-box` · `app-nav` · `badge` · `button` ·
  `chip` · `confirm` · `empty` · `field` · `list-row` · `notice` · `page` · `period-filter`.
  돈관리 전용 부품은 `src/components/fin/` 에 따로 있다.
- **`AppNav`** — 모바일은 하단 탭바, PC(lg)는 상단 헤더. **탭은 5칸: 홈 · 정비 내역 · 판매 ·
  돈 관리 · 설정.** 🔴 **5칸은 늘리지 않는다** (`/carinfo` 는 홈 소속, `/marketing` 은 설정 소속).
  작업 흐름 화면(`/sale`·`/print`·`/login`)에서는 숨긴다.
- **`useConfirm`** — 브라우저 `confirm()` 을 대체한다. **취소가 왼쪽·같은 크기**(오조작 방지),
  위험한 것은 `tone:"danger"`. 새 코드에서 `confirm()` 을 쓰지 않는다.
- **다크모드는 여전히 미지원** (D-11 7번 그대로).

**🔴 한 색 = 한 의미.** 색 의미가 충돌하고 클래스를 복붙한 것이 사고의 원인이었다.
- 성공·수입·완료 = **브랜드 초록** / 오류·지출·삭제 = 빨강 / 경고·확인필요 = amber
  (메모·임시저장·단순 안내는 slate 로 내린다) / 정보·추천 = sky
- 활성 탭·칩 = `slate-900`. **검정도 브랜드색이다 — 초록을 남발하지 않는다.**
- 카테고리 배지(계절·runflat·OE)는 **테두리형**, 상태 배지는 **채움형** — 형태로 가른다.
- 🔴 `bg-slate-900` 으로 **새 주 버튼을 만들지 않는다**. 주 버튼은 `brand-600` 이다.

**🔴 새 의존성을 추가하지 않는다.** 지금 화면 쪽은 `lucide-react` 와 Pretendard(`next/font/local`
실로딩, `src/app/fonts/PretendardVariable.woff2`)뿐이다. 아이콘은 lucide — 인라인 `size-4`,
버튼 `size-5`. 이모지는 빈 상태와 🎉 정도만.

**리포트 화면은 `src/app/reports/ui.tsx` 부품으로** (2026-09-04 4종 통일 — 매출·재고·마진·MARS).
🔴 **순위 막대를 글자 뒤에 깔지 않는다** (2026-08-07 제보로 봉인).
- **2026-09-14 손님·차량 탭 추가 (6탭)** — 탭 바는 `ReportTabs` 한 곳(새 탭은 여기에만 추가).
  자료는 `lib/report-customers.ts` · `report-vehicles.ts`(규칙 `report-cv-pure.ts`). 사장님 확정:
  **명단 없이 숫자·그래프만** · [개인·거래처·전체] 단추 · 손님당 지표는 최근 12개월 ·
  규격·인치는 2026-08부터(그 전 판매는 품목 없음) · 연료·차체는 기록 있는 차만(비율 꾸미지 않음) ·
  자리표시 이름(「고객」「관광객」) 판매는 한 사람으로 세지 않는다. `vehicle.last_fitted_size` 는 **0건인 죽은 칸**.

> 📐 **주관적인 화면은 만들기 전에 모형(ASCII)을 먼저 보여드리고 고르시게 한다.**
> 정비 내역을 두 번 만들고 나서 얻은 교훈이다 — 한 줄로 뭉갠 1차는 "여전히 별로"였고,
> 모형 3안을 드린 뒤에야 「영수증형」이 정해졌다.

### D-33. 블로그 원고는 **매장 PC 의 Claude Max 구독**으로 뽑는다 (A~D단계 2026-09-02 배포)

정본 문서 **`docs/17-네이버-마케팅.md`**. 본사 대행이 끝나 **자력으로 전환**한 것이다.

- 🔴 **`--bare` 를 쓰지 않는다** — 그 옵션은 열쇠고리를 안 읽어 **구독 로그인이 깨진다.**
  개인 설정만 떼려면 `--safe-mode` 를 쓴다 (`src/lib/ai-cli.ts`).
- 🔴 **네이버는 도구에서 하드 차단**이라 남의 블로그를 직접 못 읽는다. 그리고 **남의 글을
  자동으로 긁는 것은 약관 위반이라 애초에 안 할 일이다** (`scripts/blog-measure.ts`).
- **「AI 가 쓴 티」의 근본은 문장이 아니라 재료다.** 그래서 폼이 곧 재료다(`blog-form.ts`),
  문체 정본은 `blog-style.ts`. 🔴 **길이를 줄이는 것은 해법이 아니다.**
- **제목 검사 `titleFilter`** (`blog-style.ts`) — 진짜 결함은 제목이었다:
  | 규칙 | 값 |
  |---|---|
  | 최대 길이 | **`TITLE_MAX = 32`자** |
  | 앞머리 | **앞 `12`자 안에 「속초」 또는 작업 이름**(타이어·얼라인먼트·배터리·엔진오일…) |
  | 금지 | 계절·날씨로 시작 · 후보 3개가 서로 겹치는 것 |

  실측 근거: 발행 글 3편이 45·61·40자, 앱이 만든 제목이 38~58자 — **전부 모바일 35자에서 잘렸다.**
- 🔴 **구멍: 제미나이가 고친 글에는 앱 검사기(`styleFilter`·`titleFilter`·소제목)가 안 걸린다**
  (`blog-gemini.ts`). 다듬은 뒤에는 사람이 봐야 한다.
- 사진은 **발행용 1280px** 로 구워 끌어다 놓는다(`blog-photo.ts`). 굽는 일은 매장 PC 몫
  (`scripts/blog-publish-images.ts`).

### D-34. MARS 근본 개선 4단계 — **로봇은 정상이다. 병목은 사람이었다** ⭐ (2026-08-18~09-10)

D-08 의 「채워 넣기」를 4단계로 다시 지었다.

| 단계 | 무엇 | 근거 |
|---|---|---|
| 1 | **이중 실행 잠금** | 같은 건을 두 번 밀어넣던 것 |
| 2 | **상태를 DB 로** (`mars_run`·`mars_attempt`, 2026-08-18) | **재시도가 항상 처음부터**였다 → 시도마다 한 줄 |
| 3 | 아침 대사·자가점검 (`kind` 에 `대사`·`자가점검`) | 밤새 어긋난 것을 아침에 잡는다 |
| 4 | **번호판을 주면 검색 결과에서 「차량 줄」을 우선 고른다** (2026-08-24) | 엉뚱한 줄을 골라 멈추던 것 |

- `mars_attempt.stage` 진행 순서: **시작→고객확인→고객생성→차량생성→주문생성→줄입력→
  합계검증→전기→송장확인→점검→완료.** 어디서 멎었는지 이 값으로 본다.
- `quote.mars_status`: `미전송`·`전송완료`·`보류`·`해당없음`·`수동처리`.
- 🔴 **본사청구 예외** (`mars-queue.ts`) — 저장은 「외상」이지만 **`claim_party` 가 차 있으면
  소매다.** 이 예외가 없으면 **`claim_party` 를 만든 목적 자체가 무너진다** (D-29).
- **맥박**(`agent_heartbeat`, 열쇠 `(name, host)` — 매장 PC 2대 전제): 앱은 **10분 안에 찍혔으면
  켜짐**으로 본다. 🔴 맥박 찍기가 실패해도 **무시한다** — 맥박 때문에 본 일이 멈추면 안 된다.

**🔴 2026-09-10 점검의 결론: 고칠 것은 로봇이 아니었다.**
> 실측: 8·9월 **보류 177건 + 수동처리 2건 = 179건, 4,412만원**(가장 오래된 것 8/4).
> 그중 **122건 2,909만원은 어떤 차단 규칙에도 안 걸렸다 — 그냥 아무도 안 눌렀다.**
> 정비 내역이 「오늘」만 보여 주기 때문이다.

보류는 「오늘 아직 안 누른 것」이 아니라 **몇 달째 쌓이는 창고**가 됐다. 그래서 보류도 경고에
넣었다(`pendingCount >= 20`). **자동화를 더 하는 것보다 「안 눌린 것을 보이게」 하는 것이 답이다.**

### D-35. 회계는 **엑셀 업로드** — 대조는 전 기간(2025-01~), 한 해는 「연간 실행기」로 (2026-08-24~27)

법인은 **㈜싸이오토모티브** (내부이체 판정에 쓴다 — `expense-core.ts`).

- **파서 정본 `src/lib/fin-sheet.ts`** (ERP 1단계, 사장님 승인 2026-08-24). 받는 자료 7종:
  홈택스매출 · 홈택스매입 · 법인카드 · 통장 · 카드매출승인 · 카드매출입금 · 토스포스.
- 🔴 **파서는 폴더를 보지 않는다.** 파일은 **앱 화면 업로드**로 들어온다 — 저장소 코드 어디에도
  회계 원천 폴더 경로가 박혀 있지 않다. (원천 엑셀은 사장님 PC 에만 있고, 앱 DB 판매는
  2026-08-01부터라 **그 이전 가결산은 엑셀이 유일한 근거**다.)
- 🔴 **대조 대상은 자료 전 기간(2025-01~).** 「과거분 접기」는 폐지했다(2026-08-26, 사장님
  "중요한 건 자료"). 2025년은 앱 기록이 없으므로 **통장 직접 잇기·월정산 잔액**이 길이다.
- 🔴 **`bankLabel` 정본 `src/lib/tax-recon.ts`** — **부호(+입금/−출금)는 금액 칸에서 본다.
  시트 이름은 방향이 아니다** (「신한출금」 시트에 입금이 449줄 있었다). 날짜는 연도 포함 `YY-MM-DD`.
- **연간 실행기 `scripts/run-year.ts`** (엔진 `src/lib/year-run.ts`) — 한 해를 달마다 **화면과
  같은 규칙으로** 진행한다(자동 분류·짝 확실한 잇기·월정산 확인). 🔴 **마감은 안 누른다.**
  되돌리기는 `scripts/revert-year-run.ts` (자동·조정 자국과 월정산 확인을 원상복구).
  ⚠️ **`run-year.ts` 의 결과 JSON 저장 경로가 옛 세션 임시 폴더로 박혀 있다**(파일 끝부분).
  다시 돌릴 일이 생기면 **그 줄부터 고쳐야 「폴더 없음」으로 안 죽는다.** 계산은 멀쩡하다.

### D-36. 같은 수단 두 줄은 **정상이다** — 여러 줄일 수 있는 것을 이름으로 묶지 않는다 ⭐ (2026-09-12)

사장님 제보 두 건(Q26-0910-002 결제 · Q26-0912-001 부품)에서 나왔다. **원인이 세 겹이었다.**

권미선 손님: 1,254,000원을 **카드 3장**(200,000 + 854,000 + 200,000)으로 받았고 그중 20만원은
아들분이 늦게 오셔서 **9/12에** 받았다. 「고치기」를 열면 세 줄이 **한 칸으로 뭉개져** 한 칸만
고쳐도 셋이 같이 바뀌고 「654,000원 차이」가 떴다.

| 겹 | 무엇이 잘못됐나 |
|---|---|
| 1 | 화면이 금액을 **결제수단 이름을 열쇠로** 묶었다 — 카드가 3줄이어도 열쇠는 `"카드"` 하나 |
| 2 | 저장할 때 **받은 날짜(`paidOn`)를 안 보냈다** — 서버가 지우고 다시 넣으므로 **9/12가 지워진다** |
| 3 | 서버가 **「『카드』가 두 번 들어 있습니다」로 거부** |

🔴 **3번이 핵심이다 — 앱이 스스로 만든 자료를 앱이 불법이라고 거부하고 있었다.**
예약금을 받고 잔금을 또 카드로 받으면 `reservation-pay.ts` 가 **받은 날을 살리려고** 수금 줄을
합치지 않고 한 줄씩 `quote_payment` 로 옮긴다(D-20). 그래서 그런 판매는 **「고치기」 저장이
영영 막혀 있었다.**

**정한 것:**
- ✅ **같은 수단이 여러 줄인 것을 막지 않는다** (`payments.ts` 의 중복 검사 제거).
- 🔴 **합계 일치 검사는 절대 풀지 않는다** — D-18·D-20 이 그 위에 서 있다. 틀린 금액은 이것이 잡는다.
- 🔴 **줄을 합치는 쪽으로 풀지 않는다** — 합치면 **받은 날이 죽어** 카드 일마감이 어긋난다.
- 🔴 **`quote_payment` 를 지우는 코드는 `recon_match` 자국을 먼저 치운다.** 줄을 지우고 새로
  넣으면 id 가 바뀌어 **없어진 줄을 가리키는 유령 자국**이 남는다. `reservation-pay.ts` 는 이미
  그렇게 하고 있었고 **`sale-edit.ts` 만 빠져 있었다** — 중복 검사를 푼 이상 실제로 지나간다.

**🔴 일반 규칙 — 여러 줄일 수 있는 것을 「중복 가능한 값」으로 묶지 않는다.**
`Object.fromEntries(...map(p => [p.method, ...]))` 같은 모양이 이 버그의 정체다. 열쇠는
**줄 번호나 id** 여야 한다. 올바른 선례가 같은 폴더에 이미 있었다 — `sales/reservation-fix.tsx`.
> 전수 조사(2026-09-12) 결과 같은 종류의 진짜 버그는 **결제 줄 하나뿐**이었다. 나머지 40여 곳은
> id·복합키·`GROUP BY`·배열 누적이라 안전하다. 거래처 이름 열쇠는 `uq_supplier_key` 가 지킨다.

**곁다리 — 「고치기」 화면에도 부품을 담을 수 있다.** 「+ 타이어·부품 추가」가 검색을
`itemType:"tire"` 로 막고 있어 **배터리·필터를 담을 길이 아예 없었다**(등록 화면에는 「쓴 부품
담기」가 따로 있다). 검색을 **타이어 묶음 / 부품 묶음**으로 갈라 보여 주고, 부품 줄은 등록
화면과 **같은 규칙**으로 담는다 — `line_type='use'`, 파는 값이 있으면 그 값·없으면 0원(재고만
차감, D-25). ⚠️ 나중에 「줄을 다른 상품으로 **교체**」하는 길이 생기면 **그 검색은 타이어만**이다.

**⭐ 후속 — 이틀에 걸친 카드는 「받은 날」로 센다** (2026-09-12 같은 날, 사장님 확인 뒤)
사장님이 9/12 카드 일마감에서 잔금 20만원이 **제대로 잡힌 것**을 확인하셨다. 코드로 왜 맞는지 추적한 결과:
- 🔴 **결제 줄(`quote_payment`)의 날짜는 언제나 `COALESCE(paid_on, work_date)`** — `pos-close.ts` 의
  **`PAID_DAY`** 조각 하나(판매는 `WORK_DAY`). 손으로 다시 적지 않는다. 카드 화면도 import 해 쓴다.
- 일마감 화면·월 카드표는 원래 이 식이었는데, **월 요약 `posDaysSummary` 만 작업일로 다시 적혀 있어**
  「오늘 카드 마감」(첫 화면)·「이번 주 정리」·「월 마감」이 이틀에 걸친 결제를 「안 맞음」으로 잘못
  알렸다 — 같은 식을 두 벌 적어서 갈라진 것. 카드 화면 「차이 난 날 펼침」도 혼합 판매를 결제 줄로 싣는다.
- **매출·손익은 작업일**(발생주의, `fin-pl.ts`)이 맞고 **MARS 도 작업일 한 줄**(2026-08-15) — 갈리는 건
  **카드 대조**뿐이다. 예약금 완납 처리(`reservation-pay.ts`)는 받은 날을 보존한다.
- 정비 내역: 완납 처리로 `quote_payment` 에 옮겨진 **다른 날** 수금 줄도 재등장 재료에 넣고, **같은 판매·
  같은 날은 카드 한 장**에 「🔧 시공 완료」「💰 수금」 배지를 나란히 단다 (사장님: *"시공완료된 내역과
  수금 내역의 카드가 동시에 뜨면 안되니 한개의 카드에 같이 뜨게만"*). 같은 날 결제 줄은 넣지 않는다 —
  넣으면 보통 분할 결제까지 「그날 받은 돈」에 들어가 뜻이 바뀐다.
- 🟡 알아 둘 것: 「받은 돈 고치기」(`reservation-fix.tsx`)의 날짜 기본값은 **오늘**, 판매 등록은 **작업일**.
  「날짜·결제 고치기」 저장은 자국을 지우고 새로 넣으므로 POS 짝이 풀리지만, 다음에 일마감을 열면
  자동 대조(1:1 정확)가 다시 붙인다 — 짝을 옮기는 방식(`reservation-pay.ts:205`)이 더 좋고 별건.

### D-37. 「이번 주 정리」 한 줄 흐름 `/finance/weekly?step=N` — 돈관리 개편 3단계 ⭐ (2026-09-12)

사장님 결정 11(한 줄로 이어진 흐름)·7(확실하면 앱이 붙이고 되돌리기만 · 후보 체크해서 한 번에)·14(되돌리기는
「최근 한 일」 한 곳). 설계서 `~/.claude/plans/…distributed-orbit.md` 3-2, 실행 계획 `…enchanted-plotting-candy.md`.
**같은 날 추가 결정**: 7단계 그대로(카드 마감을 흐름에 남김) · 한 번에 배포 · **PC 전용**.

- **단계 목록·상태·남은 수의 정본은 `weekly-steps.ts` 하나** — 첫 화면 칸·흐름 진행막대·**마감 체크리스트**
  (`closeChecklist` → `weekly-close.ts closeChecksOf`)가 전부 이걸 쓴다. 전엔 마감 목록이 9줄을 따로 만들어
  **두 벌**이었다(계산서 `taxOpenCount` vs `taxOpenCounts`, 미지급 `payablesData` vs `payableTotal`).
  🔴 마감 판정 불변 — hard 3항목(입금·지출·계산서)은 같은 함수. `posclose` 항목은 카드 단계에 흡수.
- **각 단계는 3층**(`W.tierAuto`·`tierSure`·`tierCheck`·`tierHand`) — 어댑터 `weekly-deposits.ts`·`weekly-expenses.ts`·
  `weekly-payables.ts` 는 **정본 판정 결과를 층으로 나눠 담기만** 한다(순수 `partitionDeposits`·`flipExact`, 시험 있음).
  🔴 확실 3조건(`depositSurePicks`)·`exactPlan`·`resolve` 의 임계는 손대지 않았다. ⑤ 계산서는 `taxBook` 이 이미 3층.
  「앱이 자동으로 한 것」 층은 **`fin_activity` 되읽기**(`autoActivity(ym, verb)`) — `cash_txn` 에 「누가 분류했나」가
  없어 다른 길은 새 판정이 된다.
- **일괄은 서버가 다시 계산한다** — `confirmSureDeposits(ym, ids?)`·`confirmSureWithdrawals(ym, ids?)`: 화면이 보낸
  id 를 믿지 않고 `sure` 를 재계산해 **그 안에 있는 것만**(없으면 `skipped`). 낱장은 `quiet`, 끝에 **bulk 한 줄**
  (`how:"자동"`, items 로 낱건 되돌리기). `autoLinkExact` 는 코어(`purchase-pay-core.ts`)를 떼어 동작·기록 불변.
  🔴 기록은 코어 안에서만 — 액션·화면이 따로 남기면 두 줄. 새 `verb` 없음. 「이 단계 끝 ✓ · 다음 →」은 **링크일 뿐**
  서버에 안 남긴다. 마지막 「이번 주 정리 끝 ✓」만 `app_setting.weekly_done_at`(공용 `app-setting.ts` 신설).
- **`?back=weekly&step=N`** — 흐름 밖으로 나가는 링크는 「정비 내역에 등록하러」 하나. `/sales` 가 `back==="weekly"`
  ∧ step 1~7 ∧ ym 온전할 때만 돌아가는 띠(화이트리스트). 이 저장소 최초의 `?step=` 화면 — `parseStep`(1~7 아니면
  첫 미완 단계, 없으면 7). 탭 링크는 `?ym=` 만 붙어 **탭 클릭 = 첫 미완 단계**(의도).
- 기존 화면은 URL·모양 그대로 — `deposits/page.tsx` 만 조립 4줄을 어댑터 한 줄로. ui 파일은 부품 **export 승격**만.
  `month-close ↔ weekly-steps` 가 서로 import 하게 되어 읽기 `monthCloseStatusRead` 를 `month-close-status.ts` 로 뗐다.
  `revalidatePath` 직접 부르던 액션 12곳은 `revalidateFinance()` 로 통일(전부 force-dynamic 이라 과잉 무해).
- **안 한 것(4단계 몫)**: 규칙 학습 · 「올릴 때 자동 대조」 훅 · 카드 마감 인라인. 폰은 숨김만. 기존 4화면 3층 재배치는 5단계.
- 🟡 `weekly-steps` 카드 문구가 판정과 어긋나 「🟡 인데 N일 다 됨」이 마감 목록에 떴다(2026-09-12 실DB 확인) → 문구를
  cardWarn 과 같은 순서로 고침. 사용량 한도(429)로 갈래 넷이 한 번 죽었다 — 갈래는 **작은 단위로 저장**하며 진행할 것,
  파일에 제어문자(NUL)를 넣지 말 것(git 이 바이너리로 본다).

### D-38. 붙이기 자동화 + 상대별 규칙 학습 + 카드 마감 인라인 — 돈관리 개편 4단계 ⭐ (2026-09-13)

사장님 결정 7(①확실하면 앱이 붙이고 되돌리기만 ③상대별 규칙 학습)·2(**올린 직후 자동**, 단추 없음)·
4(첫 화면 「오늘」에 올리기+대조+마감 전부)·5(규칙 화면은 **보고 끄기만**)·6(앱 기본 규칙도 끌 수 있게)·
7(맞추기 옆 **체크칸 기본 켜짐**)·12(카드 일마감은 그날 저녁 사장님). 실행 계획 `~/.claude/plans/enchanted-plotting-candy.md`.
실측 근거: 한 달 정리 클릭 245회 중 **카드 일마감이 71%**, 수동 맞춤 > 자동(이체 48:8·계산서 67:32·지급 25:4).

**① 규칙 정본 `src/lib/party-rule.ts` — 다섯 갈래를 한 목록으로**
`party_alias`(이름 짝 117) · `tax_party_rule`(계산서 상대 19) · `expense_rule`(지출 230) ·
**`deposit_rule`(입금 성격, 신설)** · `DESC_RULES` 13(앱 기본 — 표가 아니라 **끈 이름만** `app_setting.desc_rules_off`).
- 🔴 **`enabled` 칸을 만들지 않았다.** `party_alias` 를 읽는 자리가 **17곳**·`tax_party_rule` 이 8곳이라
  한 곳만 빠뜨려도 **「끈 규칙이 여전히 작동」**한다. 끄기 = 그 줄 지우기, 되살리기는 「최근 한 일」의
  되돌리기(`undo.kind` 에 `ruleOn`·`ruleOff` 추가 — **새 verb 는 없다**, 기존 `verb:"규칙"`).
- 🔴 쓰기 본문을 새로 만들지 않는다 — 계산서 상대는 `recon-core` 코어(`setTaxPartyRuleCore`·
  `removeTaxPartyRuleCore`, 「경비·무시로 정리했던 계산서를 모든 달 되살리는」 논리가 거기 있다), 별명은 `learnAlias`.
- `DESC_RULES` 는 **표로 올리지 않는다** — `cond` 가 SQL 조각이라 표로 만들면 사장님이 못 고치고 위험만 는다.

**② 「☑ 다음부터 자동으로」 — 기본 켜짐, `learn` 스위치**
전엔 별명 학습이 **여덟 곳에서 조용히·무조건** 일어났다(끌 방법이 없었다). `learnAlias` 를 정본 하나로 모으고
`ActivityOpts.learn` 을 코어 6개가 흘리며, 액션 12개는 **맨 끝 선택 인자** `opts?: { learn?: boolean }`(기존 호출 동작 불변).
인라인 별명 INSERT 4곳을 이 함수로 바꾸며 🔴 **트랜잭션 밖**으로 옮겼다. 화면 체크칸은 7곳 + 일괄 3자리는
머리에 「이번 일괄은 규칙 학습 안 함」 **하나**(낱건이 아니라 그 일괄 전체 스위치).

**③ 올린 직후 자동 대조 — `auto-recon.ts`**
🔴 **올리기와 같은 요청에 붙이지 않았다.** 파일 하나 반영이 이미 질의 ~37개인데 자동 대조 세 벌을 얹으면
100+ 가 되고 다중 파일 큐면 연속이다 — 연결 자리는 3개뿐이라 **매장 앱이 멎는다**(2026-08-05·08-11 전례).
또 `applyFinUpload` 의 통짜 catch 가 자동 대조 실패를 「반영 못 했습니다」로 둔갑시킨다.
→ **화면이 큐가 끝난 뒤 두 번째 액션을 스스로 부른다**(사장님 눈엔 단추 없이 「올리면 자동」). GET 중 쓰기는 여전히 금지.
원천별 순서표(순수 `auto-recon-pure.ts`): 통장 → 카드정산·받은 돈·준 돈 / 홈택스 → 두 방향 /
법인카드·토스포스·카드매출 → **없음**(각자 ingest 가 이미 한다 — 토스포스는 파일에 든 날마다 `autoMatchPosDayCore`).
🔴 기록은 코어가 bulk 한 줄씩 — 이 액션은 아무것도 안 남긴다.

**④ 카드 마감 인라인** — `pos-close-ui.tsx` 486줄에서 조각을 떼어(`card/pos-parts.tsx`) 첫 화면 「오늘」 칸이
**같은 부품·같은 단추**를 쓴다(재조립 후 296줄, 액션 11개 전부 보존). 🔴 `posDayData`(순차 12~19질의)는
**조건부로만** — 오늘 POS 자료가 있고·마감 안 했고·남은 건이 있을 때. 평소 질의 증가 0.
「안 된 날 N일」이 링크가 되어 정본 `nextOpenDay`(순수 `pos-close-pure.ts`)로 다음 날로 바로 간다 —
첫 화면·카드 화면·이번 주 정리 ② 가 **같은 순서**를 본다. `nextOpenDay` 에 오늘을 `current` 로 넘기는 이유:
안 넘기면 오늘이 답이 되어 **옆 「대조 →」 단추와 같은 곳**으로 가는 링크가 된다.

**⑤ 정합성 정리(보이는 숫자 불변)** — `applyAutoCategories` 는 카드정산에 `category` 만 찍고 `recon_status` 를
미대조로 뒀는데 `setDepositKind`·`markCardSettlements`·`unmark` 는 두 칸을 같이 다뤘다. 화면·남은 수 판정은
처음부터 `category` 기준이라 숫자는 그대로지만, **되돌리기가 제 짝을 못 찾던** 반쪽 줄 **1,854건**을 맞췄다
(`scripts/add-deposit-rule.ts`, `--dry` 로 건수 먼저 보고 실행).

**🔴 배포 시점 실DB 확인(2026-09-13)** — 자동 확정 후보가 **계산서·준 돈·카드정산 전부 0건**,
열린 입금도 8·9월 0건이었다. 즉 **배포 직후 아무 자료도 안 바뀐다**(다음 올리기부터 효과).
그래서 계획서가 잡았던 「별명→판매 확장으로 자동 확정이 몇 건 늘어나나」는 **측정하지 못했다** —
안전성은 구조로만 보장된다(`depositSurePicks` 3조건·`exactPlan`·`sureTaxPicks` 본문 불변, 별명은
「식별 확실」의 **입력원**만 넓힌 것으로 계산서 ★ 와 같은 취급). **첫 통장 올리기 때 결과를 봐야 한다.**

**안 한 것(5단계)**: 규칙 직접 추가·수정 UI · `DESC_RULES` 표 승급 · `party_alias`·`expense_rule` `enabled` 칸 ·
~~지역화폐·이자·환급 단계의 `recon_status` 통일(undo 경로 정비가 먼저)~~(5단계 D-39 에서 함) · 카드 인라인의 묶어 붙이기·선결제·수동 select ·
`setDepositKind` 과거분 일괄 · 자동 연동 · 폰(보기만).

### D-39. 겹친 화면을 걷어낸다 — 돈관리 개편 5단계 「정리」 ⭐ (2026-09-13)

1~4단계로 첫 화면 세 칸·`/finance/weekly` 흐름·「최근 한 일」·자동 규칙이 생기면서 **같은 정본을 두 곳이
다르게 그리는** 자리가 남았다. 새 기능 없이 그것을 걷어내고 미룬 부채를 갚았다. 사장님 결정 11개
(설계서 §2·§4 표 5행, 실행 계획 `~/.claude/plans/enchanted-plotting-candy.md`). **한 번에 배포.**

**① 없앤 것** — 첫 화면 정합성 배너(`audit-banner.tsx`)·상대별 인박스(`inbox-ui.tsx`·`fin-inbox.ts`)·
**추적 화면 전체**(`/finance/trace`·`money-trace.ts`, 탭에서도 제거). 코드 −1,854줄.
- A1(안 들어온 이체)은 「오늘」 칸이 **같은 함수**(`a1OpenTransfers`)로 이미 보여 줬다 — 「나머지 N건 →」이
  추적으로 보내던 것은 **그 자리에서 펼친다**(서버 `<details>`, 클라이언트 상태 없음).
- A2~A4 는 **장부 마감 체크리스트 한 줄**(`month-close.auditCheck`, soft)과 `/finance/ledger#audit` 접힘으로.
  🔴 **`freshAuditRun` 을 지웠다** — 지문이 바뀌면 GET 중에 `INSERT audit_run` 을 하고 A1 이 판매 60건×후보라
  첫 방문에 60+ 질의였다. 이제 **읽기 2질의**(`latestAuditRun`+`auditFingerprint`)로 「N가지 · 자료가 바뀜 —
  다시 검사」라고 **적기만** 하고, 다시 돌리는 건 07:30 cron 과 장부 「지금 다시 검사」 단추(`ledger/audit-rerun.tsx`) 둘.
- 저장된 `audit_run.items` 에 옛 주소가 남으므로 화면은 저장본 href 대신 **`AUDIT_HREF` 표**(self-audit.ts)로 그린다.
- 옛 「통장 밖 수령」 자국 **35건**(2단계 전이라 `fin_activity` 줄이 없어 추적 화면이 유일한 되돌리기 자리였다)은
  `scripts/add-aside-activity.ts` 로 「최근 한 일」에 옮겨 적었다. 🔴 postgres.js 에서 `${JSON.stringify(x)}::jsonb` 는
  **한 번 더 감싸진다**(`"{\"quoteId\":13}"`) — `sql.json(x)` 를 쓸 것(스크립트에 수리 단계 포함).

**② 기존 4화면 = 흐름과 같은 3층** (URL·딥링크 그대로, 사장님 결정 3)
`/finance/deposits`·`/expenses`·`/payables` 가 page 에서 어댑터(`weekly*Step`)를 한 번 부르고 흐름 조각
(`DepositsFlow`·`ExpensesFlow`·`PayablesFlow`)을 그린다 — 옛 `DepositsRecon`·`ExpensesUi`·`PayablesUi` 삭제.
`weeklySteps` 는 안 부르므로 **조회가 늘지 않는다**(⑥ 은 5개 → 1개). ⑤ 계산서는 1.5단계에 이미 교체됨.
- 🔴 서버 컴포넌트는 클라이언트 조각에 **함수를 못 넘긴다** — `saleHref` 대신 `standalone?: boolean`(흐름이면 `back=weekly`).
- 장부 성격 조각은 「자세히」 접힘으로: ④ 분류별 합계(어댑터가 `expenseData` 를 통째 주므로 조회 0) ·
  ⑥ 거래처 카드에서 **남긴 것**(손 지급 폼·선급금·✅지급 확인·매입별 잔액)은 `SupplierCards`.
  **뺀 것**: ⚡낱건 자동대조(흐름 `sure` 층이 같은 `exactPlan` 을 일괄로)·별명 관리(설정→자동 규칙에 있다)·
  `taxCashData` 안내 박스(수십 질의)·`byPayer` 묶음(흐름 `check` 가 더 정확한 열쇠로).
- 탭: **할 일 = 현황·이번 주 정리·최근 한 일** / 장부 = 손익·거래처·입금·지출·계산서·미지급·받을 돈.
  계산서 `?view=money` 특례 제거(`tax/page.tsx` 가 무시한 지 오래) — 링크 7곳 정리.

**③ 부채**
- `ledger/page.tsx` 인라인 SQL 8개 → **`src/lib/ledger-data.ts`**(`ledgerCoverage`·`ledgerOpenBuySum`·`ledgerMonthSums`)
  + `FinPL.salesN`. 🔴 **숫자를 바꾸는 이관은 안 했다** — `ledgerOpenBuySum` 을 `taxOpenCounts` 로 합치면
  「장 합 vs 남은 돈」이 달라진다. `bySource`/`accounts` 도 LIMIT·noData 판정이 미묘해 한 질의로 안 합쳤다.
- `app_setting` 사설 4곳(`mars-eval`·`invoice-deadline`·`invoice-deadline-actions`·`battery-price`) → 공용 `app-setting.ts`.
- **`recon_status` 통일 + 되돌리기 버그**: 자동 분류의 지역화폐정산·이자·환급이 `category` 만 찍어
  ⓐ 「제안」(후보가 붙은 줄)을 조용히 덮고 ⓑ 반쪽 줄이 남았다. 셋 다 `recon_status='확정'` + `AND recon_status='미대조'`
  가드로 카드정산과 같은 모양. 🔴 그 전에 **되돌리기 길부터**: `UNDO_DEPOSIT_KINDS`(고르는 넷 + 「지역화폐정산」) —
  지역화폐는 `expense` 로 가는데 `fin-expense` 가 입금 줄을 거부해 **되돌리기가 늘 실패했다**.
  과거 반쪽 줄 23건은 `scripts/add-recon-status-backfill.ts`(화면 숫자 불변 — 판정이 category 기준).
- `taxBook` 에 `PartyCashCache` 주입(`partyStrictNames`·`partyMonthlyCash` 결과 메모, 요청 안에서만) — 실측 **67 → 63 질의**.
- 시험 3개 추가(`expense-cats`·`ym`·`payer-name`, +150) → **606 통과**. `expense-cats` 는 TS `payerKeyOf` ↔
  `PAYER_KEY_SQL` **쌍둥이**를 SQL 실행 없이 지킨다(조각 순서·`\[` 이스케이프 존재·접두사 누락 0).

**안 한 것(별건)**: `card/page.tsx` 인라인 SQL(:67 「혼합은 결제 줄로」가 `cardDaySums` 와 물려 위험) ·
`posDayData` 경량화 · 규칙 추가·수정 UI · 다중 올리기 자동 진행 · 카드 인라인 빈 슬롯 · `taxOpenCounts` 합계 칸(숫자가 바뀐다).
**뒤따라 고친 것(09-14)**: `payer-name.ts readPayer` — 결제대행을 벗긴 이름이 음식점이어도 결제대행 사전 fallback 이
먼저 `known` 을 채워 「식대·접대」 제안이 안 나왔다(네이버페이·카카오페이 밥값). 음식점 판정을 fallback **앞**으로.
실측: 지금 DB 에 해당 줄 0건(결제대행 줄은 판매처 없이 `네이버파이낸셜(주)` 로 온다) — 앞으로 들어올 줄 대비.

---

## 미결 사항  *(2026-09-12 사장님과 함께 정리)*

### 🔴 남은 것 — 사장님 답이 필요하다

- [ ] **미쉐린 외 브랜드 기표가도 VAT 미포함인가?** *(사장님 2026-09-12: "잘 모르겠다 — 확인 필요")*
      **금호·콘티넨탈은 해결됐다** — 거래처 자료로 들어오고 성격이 코드에 박혀 있다
      (금호 자재검색 공장도가 = VAT 포함 / 금호 기표가 Master·콘티 두 파일 = VAT 미포함 ×1.1).
      🔴 **남은 것은 MARS 수출본으로만 들어온 브랜드**(피렐리 1,535 · 한국 1,399 …).
      **확인법**: 그 브랜드 청구서 한 장의 단가와 앱 기표가를 맞춰 본다. 다르면 「상품 정리」
      화면에서 브랜드 스위치(`price_excludes_vat`)를 켜면 즉시 재계산된다 (D-05 보강).
- [ ] **법인 외상 계정 매출이 0인 이유** — 외상 거래를 MARS 에 안 잡으시는지
- [ ] 서비스 단가가 **현재 실제 청구액과 같은가** (최종 수정 2024~2025년)
- [ ] MARS 「매입처」 내보내기 — 발주처 확인 (V001/00010이 주력)
- [ ] **부품 매입가·판매가** — 배터리는 단가표로 해결됐지만(D-25) **필터·패드는 여전히 0원**이다

### ⏸ 보류 — 하기로 했으나 지금은 아니다 *(사장님 2026-09-12)*

- [ ] 🅿️ **타이어 보관 서비스** — **만들기로 하셨다.** 다만 착수는 나중. 잠자는 고충 5순위이고
      위치·목록·만료청구·재방문유도 **넷 다 미구축**이다. 시작할 때 함께 정할 것:
      **① 보관료 단위**(시즌당 / 연간 / 건당 — 옛 메모의 150,000원은 단위가 불명)
      **② 자리 표기**(층을 쓰는지 · 랙·단을 어떻게 부르는지)
      **③ 보관번호 스티커**(D-03) — 🔴 D-11 4번에서 바코드를 접으면서 **라벨로 한 본을 특정한다는
      안이 붕 떠 있다.** 보관을 만들 때 이것부터 정해야 한다.
- [ ] **문자 알림** — 쓸 생각은 있으나 지금은 아니다. 시작하려면 **발신번호 사전등록**이 먼저다
      (휴대폰은 99.6% 확보돼 있어 기술적 장애는 없다 — D-10).
- [ ] 판매 플랫폼 데이터 수집 — 아래 스크래핑 주의사항을 먼저 볼 것.

### ✅ 2026-09-12 정리에서 닫은 것

- [x] ~~폐타이어 처리비~~ — **안 받으신다.** 항목을 만들지 않는다.
- [x] ~~휠밸런스 "2개당"이 맞는가~~ — **자동으로 올리지 않는다.** 사장님: *"타이어를 입력하면
      자동으로 휠타이어 교환이나 휠밸런스가 올라가는데 그럴 필요 없음. **그냥 타이어 값에 보통
      포함**되거든."* 따로 받으실 때만 「공임·정비 추가」로 넣는다. `qty_rule = per_2_units`
      규칙은 **잠들어 있다** — 나중에 필요해지면 그 값으로 계산하면 된다 (`sale.ts`).
- [x] ~~고객 등급 명칭과 등급별 추가 할인율~~ — **등급을 안 쓰신다. 그때그때 정하신다.**
      D-05 5번(현장 재량 오버라이드)이 그대로 답이다.
- [x] ~~매입 할인율이 매입처별로도 다른가~~ — **상관없다.** 화면에서 금액을 직접 고칠 수 있다.
      `price_rule` 에 「매입처」 축을 더하지 않는다.
- [x] ~~1층/2층 구분을 쓰시는가~~ — **보관 서비스를 설계할 때 함께 정한다** (위 보류 항목).
- [x] ~~`126081` DOT 합 31 ≠ 수량 27 · `520336` 수량 0인데 DOT 2본~~ — **이관 시점(2026-08)
      자료다.** 그 뒤 실사를 했고 지금은 실사·소급(D-26)이 기준이라 이 두 건은 의미가 없다.

<details>
<summary>2026-08 착수 전에 닫았던 것들 (펼치기)</summary>

**✅ 착수 조건 전부 해소 — 1주차 이관 착수 가능 (2026-08-01)**
- [x] ~~미쉐린 `26.7.16재고UDT`가 최신인지~~ — ✅ **최신 확정. `틀` 시트 폐기** (사장님 확인)
- [x] ~~자동 백업~~ — ✅ **구성 완료** (2026-08-05). 매장 PC 작업 스케줄러
      「타이어모어 DB 백업」이 매일 13:30 `scripts/backup-db.bat` 실행 (놓치면 PC 켜질 때
      보충). 성공 기록은 `backup_log` → 설정 화면이 이틀 넘으면 경고.
      로그: `tyremore-data\backup\backup.log`.
- [x] ~~API 키 스크린샷 정리~~ — ✅ **삭제 완료** (2026-08-05, 사장님 지시).
      본사 API 키가 찍힌 스크린샷은 더 이상 없다. 정보는 `docs/04` 2장에 정리돼 있다.
- [x] ~~`hisonic001/MARS-auto-register` 공개 저장소~~ — ✅ **비공개 전환 완료**
      (2026-08-05, 사장님 지시). 고객 실명·전화가 든 엑셀이 있던 옛 저장소다.
- [x] ~~오일필터 시트 확정~~ — **불필요.** 부품 수량 자체를 안 쓴다 (D-12 5번)

- [x] ~~미쉐린 외 브랜드 재고~~ — **해소.** 사장님이 2주차 입고·실사 화면으로 직접 입력 (D-12 6번)

**해소됨 (2026-07~08)**
- [x] ~~할인율 가이드라인 수치~~ — 공란 시작 + 화면 입력 방식 (D-05)
- [x] ~~MARS 고객·차량 내보내기~~ — **완료.** 연락처 2,603 / 차량 2,574 (D-10)
- [x] ~~부대비용 항목~~ — **완료.** 패스트핏 서비스 69건에 전부 있음 (D-10)
- [x] ~~자주 쓰는 경정비 항목·공임~~ — **완료.** 국산/수입 구분까지 확보 (D-10)
- [x] ~~보관료 요금 체계~~ — 윈터타이어 보관료 150,000원. **단위는 아직 미정** (위 보류 항목)
- [x] ~~본사 IT 협의 / PartZone 문의~~ — 불필요. 본사 협의 불가(D-08), 외부 API 미도입(D-04)
- [x] ~~MARS 실사 확인~~ — D-08·D-09 참조
- [x] ~~기표가표 확보~~ — 상품 마스터 `단가1` (D-09)

</details>

> ⚠️ **개인정보**: 연락처·차량 내보내기 파일에는 실명과 휴대폰 번호 2,593건이 들어 있다.
> **이 폴더 밖으로 공유하지 말 것** (메신저·이메일·공개 저장소 포함).

> ⚠️ **스크래핑 주의**: 사용자가 "판매 플랫폼에서 스크래핑하면 데이터를 얻을 수도 있다"고 언급함.
> 자사 계정의 자사 거래·정산 내역을 내려받는 것은 대체로 문제없으나,
> 타사 상품·가격 데이터를 대량 수집하는 것은 해당 사이트 이용약관 위반 소지가 있다.
> 진행 전 반드시 목적과 대상을 확인하고, 공식 API·정산 파일 다운로드 등 정규 경로를 먼저 검토할 것.

---

## 기술 스택 (**확정** — D-11 / `docs/08`)
| 영역 | 확정 | 선정 이유 |
|---|---|---|
| 언어 | TypeScript | 오타·누락을 실행 전에 잡는다 |
| 프레임워크 | **Next.js 15 (App Router)** | 화면+서버 단일 코드, 폰·태블릿·PC 반응형 |
| DB | **PostgreSQL 16** | 표준 SQL. 클라우드→자체서버 이관이 쉬움 (D-06) |
| DB 접근 | Drizzle ORM | SQL에 가까워 떼어내기 쉬움 |
| 인증 | Auth.js (자체 세션) | 특정 업체 인증에 묶이지 않음 |
| UI | Tailwind + shadcn/ui | 코드가 프로젝트 안으로 들어옴. 버전 종속 없음 |
| 배포 | Vercel + **Supabase 서울 리전** | 관리 부담 최소, 월 0~7만원. **개인정보 국내 저장** |
| ~~바코드 입력~~ | ~~USB/블루투스 리더기~~ | 🔴 **제외 (2026-08-04)** — 써보니 불편. 품목 검색으로 대체 (D-11 4번) |
| 라벨 출력 | 라벨프린터 + 웹 인쇄 | 보관번호 스티커 (D-03) · 2개월차 |

---

## 작업 규칙
- 결정이 바뀌면 **결정 기록을 수정**하고 이유를 남길 것. 문서와 코드가 어긋나면 안 됨.
- 산출물은 `docs/` 아래에 번호를 붙여 관리한다.
- 현장 사용자는 정비사다. **화면은 장갑 낀 손과 더러운 화면을 전제**로 크게, 단순하게.

---

## 병렬 작업 규칙 (2026-09-10 · 사장님과 합의)

여러 갈래(subagent)를 동시에 돌릴 때의 규칙. **어기면 매장 앱이 멈추거나 고친
것이 사라진다.**

### 사장님이 부르는 법 — 외울 명령어는 없다

`/agents` 는 **실행 명령이 아니라 정의 파일을 관리하는 것**이다. 사장님은 그냥
말씀하시면 되고, 띄우는 것은 Claude 가 한다(한 메시지 안에서 Agent 를 여러 번
부르면 그대로 동시에 돈다).

| 이렇게 말씀하시면 | 이런 갈래가 돈다 |
|---|---|
| **"운영 점검 돌려줘"** | 자료 사진 한 장 → 마진·재고·세무·운영 **4갈래 동시** → 검증 후 묶음 보고 |
| "세무만 봐줘" / "재고만" | 그 갈래 하나만 |
| "이거 조사해줘" (넓은 주제) | 조사 갈래 2~3개 |
| "이 셋 동시에 고쳐줘" | **겹침표 보여드리고 승인 후** worktree 갈래 |
| "지금 뭐 돌고 있어?" | Claude 가 목록을 보여줌 (사장님은 **`/tasks`** 로도 확인·중단 가능) |

### Claude 가 지킬 것 (사장님이 매번 안 챙기셔도 되게)
1. **띄우기 전에** 무엇을 몇 갈래로 나눌지 쉬운 말로 보여주고 승인받는다.
2. 동시 **3~4개**. 분석 갈래는 **DB 금지**(자료 사진만).
3. 갈래가 낸 말을 **그대로 전하지 않는다** — 결정에 직결되는 숫자·인과는
   실DB 로 검증한 뒤 보고한다. (첫 실전에서 갈래 둘이 틀렸다: MARS 미전송을
   「거래처 탓」이라 했으나 실제는 일반 소매였고, 재고 잠김을 4,700만이라
   했으나 실측 3,581만이었다.)
4. 갈래 결과는 **한 장으로 묶어** 급한 순으로 보고한다.

### 🔴 새로 만든 갈래 정의는 그 세션에서 안 잡힌다
`.claude/agents/*.md` 를 새로 만들면 **다음 세션부터** 이름으로 불린다
(실측: `Agent type 'ops-margin' not found`). 당장 써야 하면 정의 내용을
general-purpose 갈래 프롬프트에 넣어 우회한다.

### 🔴 DB — 한 번에 하나
실DB 는 하나뿐이고 연결 풀이 **3**이다. 갈래 넷이 동시에 조회하면 풀이 만석이
되어 **매장 앱이 전면 마비**된다(좀비 질의 3개로 실제로 겪음).
- **분석 갈래는 DB 를 안 쓴다.** `npx tsx --env-file=.env.local scripts/ops-snapshot.ts`
  로 **자료 사진**을 한 번 떠서 `.snapshot/<시각>/` 에 두고, 갈래는 그 파일만 본다.
- 실DB 조회·수정·마이그레이션은 **주 세션에서 한 번에 하나씩**. 갈래 안에서는 금지.
- 사진의 판정은 기존 정본을 호출한다(`margin-def` · `stock-integrity` ·
  `receivable-book` · `rebate`). 사진이 화면과 다른 숫자를 말하면 안 된다.

### 🔴 나누기 전에 겹침을 재고 승인받는다
앱 기능 하나가 평균 4개(큰 건 16개) 파일을 건드리고 `sale.ts`·`sale-edit.ts`
같은 **공용 파일을 자주 지난다**(최근 40커밋 실측).
1. 갈래별로 「건드릴 파일 목록」을 먼저 적는다.
2. 겹치면 **동시에 안 돌린다 — 줄을 세운다.**
3. 안 겹치는 조합을 사장님께 **쉬운 말로 보여주고 승인**받은 뒤 띄운다.
4. 동시 **3~4개**가 상한.

### 🔴 빌드·테스트는 주 세션에서 직렬
`esbuild.exe` 잠금(EPERM)과 `.next-prod` 캐시 손상(WasmHash) 전례가 있다.
갈래는 `tsc --noEmit` 까지만, `npm run build`·`npm test` 는 주 세션이 한 번.

### 고치는 일을 병렬로 — 6단계 (worktree)
읽고 보고서만 쓰는 갈래는 worktree 가 **필요 없다**(만드는 비용만 든다).
파일을 고치는 갈래만 이 절차를 탄다.

1. 갈래별로 **건드릴 파일 목록**을 먼저 적는다.
2. **겹침표**를 만들어 사장님께 보여드린다 — 「이 셋은 안 겹칩니다」.
3. **겹치면 동시에 안 돌린다 — 줄을 세운다.** (판단이 애매하면 줄 세우는 쪽)
4. `Agent(isolation:"worktree")` 로 띄운다. 갈래는 **`tsc --noEmit` 까지만** —
   테스트·빌드·커밋은 하지 않는다.
5. 끝나면 **주 세션이 순서대로 합치고**, 거기서 한 번만 테스트·빌드·커밋한다.
   커밋은 **경로 명시**(`git add -A` 금지) — 병렬이면 더욱.
6. worktree 는 **바로 지운다**(`git worktree remove` + 브랜치 정리). 아무것도
   안 고치고 끝난 worktree 는 알아서 치워지지만, 고친 게 있으면 남는다.
   묵은 껍데기가 남으면 다음 사람이 "아직 진행 중인가" 헷갈린다.

겹침을 미리 걸렀으므로 **충돌이 났다면 그건 계획이 틀린 것**이다 — 억지로
풀지 말고 되돌린 뒤 다시 나눈다.

### 운영 정기 점검 — 갈래 넷
`.claude/agents/ops-{margin,stock,tax,ops}.md`. 사진 경로를 알려 주면 각자
읽고 「문제 3개 · 근거 숫자 · 고칠 방법 · 안 고치면 얼마 손해」를 보고서로 낸다.
**보고서까지만** — 고치는 것은 사장님이 고른 뒤에 따로. 기존 `store-advisor`
(실DB SELECT 전용)는 단발 질문용으로 그대로 둔다.
