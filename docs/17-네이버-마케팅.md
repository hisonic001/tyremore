| `npm run blog-draft -- --dry` | 후보와 지시문에 들어갈 사실만 출력 (모델 안 부름) || `npm run blog:agent` | **매장 PC 대리인.** 앱의 주문(`blog_job`)을 5초마다 받아 구독으로 원고를 만든다. `mars-agent.bat` 이 같이 띄운다 || `/marketing/blog` | 초안 목록. 「오늘 원고 만들기」 → **매장 PC 대리인**이 만든다. 진행 로그가 화면에 흐른다 |# 네이버 마케팅 — 블로그 초안 · 리뷰 답글 · 유입경로 (2026-08-29)

속초점은 네이버 블로그(`tyremore_sokcho`)·스마트플레이스(`1923403565`)·검색광고(월 25만원)를
다 쓰지만 전부 손으로 돌아갔다. 이 문서는 그중 **프로그램이 안전하게 도울 수 있는 부분**만 다룬다.

계획은 SEO 전문가·검색광고 마케터·현장 운영자 세 시각으로 검토했다(2026-08-29). 그 결과 처음 안에서
**뺀 것이 더 많다** — 아래 「안 만드는 것」 참조.

---

## 🔴 원칙

| | |
|---|---|
| **발행·답글 등록은 사장님 손** | 블로그·플레이스에 프로그램이 올리면 매크로 판정 → 저품질. `docs/15` MARS 「전기는 안 한다」와 같은 원칙 |
| **개인정보는 앱을 떠나기 전에 지운다** | API 로 나가는 건 차종·연식·주행거리**대**·규격·본수·계절뿐. 번호판·이름·전화·정확한 km·날짜·사진은 안 보낸다. 출력도 정규식으로 한 번 더 거른다 |
| **광고비가 저절로 움직이는 일은 없다** | 검색광고 API 는 광고비 월 50만원 넘으면 붙인다. 그때도 입찰가 자동조정은 안 한다 |

---

## 무엇이 어디에

| 화면 / 명령 | 하는 일 |
|---|---|
| 판매 등록 → 결제 칸 「어떻게 알고 오셨어요?」 | `quote.referral`. 네이버 검색·플레이스·소개·지나가다·재방문·기타. **선택** |
| 홈 상단 「블로그 초안 N개」 카드 | 사장님 계정에만, 안 올린 초안이 있을 때만 |
| 설정 → 마케팅 (`/marketing`) | 블로그 초안 · 리뷰 답글 초안 · 먼저 손으로 할 일 |
| `/marketing/blog` | 초안 목록. 「오늘 초안 만들기」로 크론 안 기다리고 지금 |
| `/marketing/blog/[id]` | 제목 고르기 → **사장님 한마디(15자 이상, 필수)** → 복사 → 블로그 앱에 붙여넣기 |
| `/marketing/reply` | 리뷰 붙여넣기 → 답글 초안 → 복사. 저장 안 함 |
| `/api/cron/blog-draft` | 매일 21:00 KST (Vercel 크론). 그날 시공에서 최대 3건 |
| `npm run blog-draft -- --dry` | 후보와 지시문에 들어갈 사실만 출력 (API 안 부름) |
| `npm run blog-draft -- --limit 1` | 처음 시험할 때 1건만 |

---

## 블로그 초안이 만들어지는 과정 (`src/lib/blog-draft.ts`)

1. **글감 고르기** `pickCandidates` — 그날 성사 판매 중 타이어가 있는 것. 거래처·무상(서비스)·이미 초안 있는 건 제외.
   4본 교체 우선, 같은 차종은 하루 하나.
2. **사실 만들기** `factsText` — 지시문에 들어가는 전부. `--dry` 로 눈으로 확인할 수 있다.
   ```
   차량: 현대 그랜저 2019년식, 주행 8만km대
   시공: 미쉐린 프라이머시4 235/45R18 4본
   시기: 8월 말, 한여름
   ```
   주행거리는 만 단위로 뭉개고 날짜는 계절로만 — 손님 특정을 막는다.
3. **최근 글 제목 읽기** — 공개 RSS(`rss.blog.naver.com/tyremore_sokcho.xml`). 같은 차종 글이 있으면 **경고**만 붙인다.
4. **생성** — `claude-opus-5`, JSON 고정 출력(제목 3·본문·태그). 글 구조 4가지를 돌려 쓴다(같은 틀 반복 = 유사문서).
5. **2차 필터** `privacyFilter` — 번호판·전화 패턴, 그 손님 이름이 나오면 **버리고 한 번 다시**, 또 나오면 실패.
6. 본문에 `{{사장님_한마디}}` 자리 — 없으면 끼워 넣는다. 화면은 이 자리에 15자 이상 쓰기 전에는 복사를 안 켠다.

**지시문의 SEO 규칙** (SEO 전문가 리뷰 그대로): 결론 먼저 · 속초 2~3회만 · 소제목 3~5개 + 표/체크리스트 1개 ·
1,200~1,800자 · 가격은 범위 · 최상급 금지 · 태그 10개 이하 · 끝에 「지도 붙이세요」.

**비용**: 글 1건 ≈ 40원. 하루 3건이면 월 4천원 안쪽.

---

## 준비 (처음 한 번)

```
npx tsx scripts/add-blog-draft.ts        ← 코드 배포 전에. quote.referral + blog_draft
```

`.env.local` 과 Vercel 환경변수:

| 이름 | 어디서 |
|---|---|
| `ANTHROPIC_API_KEY` | console.anthropic.com |
| `CRON_SECRET` | 아무 긴 문자열. Vercel 이 크론 요청에 실어 보낸다 |

⚠️ 깃허브에는 안 올라간다 (`.env.local` 은 `.gitignore`).

Vercel 함수 시간: 초안 하나에 30~60초. 크론 라우트는 `maxDuration = 300`. 플랜 제한에 걸리면
매장 PC 작업 스케줄러에서 `npm run blog-draft` 를 21:00 에 돌리면 같은 일을 한다.

---

## 2주 시험 뒤 판단 (현장 운영자 리뷰)

초안 20~30개 중 **실제 발행 5개 이상**이면 다음을 붙인다:
- 결제 완료 화면에 **카메라 버튼** 1~2장 → 폰에서 번호판 블러 → 블러본만 저장 (발행 때 사진 재선택 문제 해결)
- 판매 화면 「사진 게시 동의」 토글
- 웹 푸시 「초안 나왔습니다」 (지금 앱엔 푸시 인프라가 없다)

거의 안 쓰면 크론을 주 2회로 줄이고 여기서 멈춘다.

---

## 안 만드는 것 (그리고 왜)

| 처음 안 | 결론 | 이유 |
|---|---|---|
| 클로드 + 제미나이 두 벌 | 클로드 한 벌 + 「다르게 한 번 더」 | 3인 전원: 폰에서 덩어리 8개는 선택 피로. 사장님 확정 |
| 플레이스 리뷰 Playwright 자동 수집 | 뺌 | 플레이스 앱 푸시가 무료로 알려 준다. 셀렉터 유지비 > 효익 |
| 검색광고 API 수집·리포트 | 광고비 50만↑ 때 | 월 25만원이면 개발 공수 못 넘김. 대신 계정 수동 점검 1회 |
| 광고비 ÷ 신규 고객 (ROAS 추정) | 뺌 → `referral` 실측 | 분모에 소개·재방문이 섞여 틀린 숫자. 「어떻게 알고 오셨어요?」가 더 정확 |
| 사진 올리고 판매 건 고르기 | 사진 없이 데이터만으로 | 구리스 손으로 5장·재진입은 포기 지점. 사진은 시험 통과 후 |

검색광고 API 를 붙일 때 알아 둘 것(마케터): StatReport 는 비동기·새벽 지연 → 09:30 이후 실행,
`(날짜+키워드)` upsert 로 최근 7일 재수집(무효클릭 소급), 대조는 관리화면을 **검색광고로 필터링**한 상태에서만
(2026-03 통합 개편으로 총액이 다르다). 이 규모에서 지렛대는 입찰가가 아니라 **제외 키워드·지역·시간대**다.

---

## 먼저 손으로 할 것 (개발 없음, 반나절)

- 플레이스: 대표키워드·업종·영업시간·임시휴무 정확히, 예약/톡톡 ON, 소식 주 1회, **리뷰 요청 QR**
- 검색광고: 계정 1시간 — 제외 키워드·지역·시간대, 비용 상위 5개 키워드
- 블로그 글 끝에 장소(지도) 첨부, 플레이스 소식에 그 글 링크 — 양방향

---

## v3 1단계 — 구독으로, 앱에서 (2026-09-02)

사장님이 **Claude Max 를 이미 구독 중**이라, 2주 시험은 API 키 없이 그 구독으로 한다 (요금 0원).
쓸 만하다고 판단되면 그때 키를 넣어 API 로 올린다 — 그때 고칠 곳은 `src/lib/ai.ts` 하나뿐이다.

### 왜 매장 PC 가 필요한가

구독은 **그 PC 에 로그인된 Claude Code** 를 통해서만 쓸 수 있다. Vercel 에는 그 로그인이 없다.
그래서 MARS 와 똑같은 다리를 둔다:

```
[앱 버튼]  →  blog_job 「대기」  →  [scripts/blog-agent.ts]  →  claude -p (구독)
                    ↑                        └ 진행 로그를 되쓴다 ─┐
              [앱이 3초마다 확인] ←──────────────────────────────┘
```

- `blog_job` — 주문표. `mars_run` 과 같은 모양이되 **표를 따로 뒀다**: MARS 입력은 최대 70분
  「한 번에 하나」라, 그 뒤에 서면 1~3분짜리 원고를 한 시간 기다리게 된다. 자원도 안 겹친다.
- `agent_heartbeat` — 대리인이 15초마다 찍는다. 60초 넘으면 앱이 **「매장 PC 꺼짐」**을 띄우고
  주문 자체를 안 받는다 (「대기」로 굳어 영문을 모르는 일을 막는다).
- 집는 방식은 `FOR UPDATE SKIP LOCKED` — 창이 두 개 떠도 같은 주문을 두 번 처리하지 않는다.
  자문 잠금과 달리 연결을 놓아도 안전해, 풀러 자리를 오래 물지 않는다.

### `claude -p` 를 부를 때 지키는 것 (`src/lib/ai-cli.ts`)

| | 이유 |
|---|---|
| 지시문은 **표준입력(UTF-8)** 으로, 인자는 전부 ASCII | 윈도우 argv 는 코드페이지를 타서 한글이 깨질 수 있다 |
| `.cmd` 가 아니라 **`claude.exe` 를 직접** (shell 없이) | 새 Node 는 shell 없이 `.cmd` 를 못 돌린다 |
| **`--bare` 금지**, 대신 `--safe-mode` | `--bare` 는 열쇠고리를 안 읽어 **구독 로그인이 깨진다**. API 키 전용이 된다 |
| `--json-schema` + 실패 시 울타리 벗기기 → 스키마 없이 1회 재시도 | 형식이 어긋나도 한 번은 건진다 |
| `--tools ""` | 글만 쓰게 한다. 파일을 뒤지지 않는다 |
| 쓴 모델은 **출력 토큰이 가장 많은 것**으로 기록 | 첫 키를 쓰면 곁다리 일에 쓰인 작은 모델이 기록된다 (2026-09-02 실제로 haiku 로 잘못 남았다) |

### 오늘이 비면 밀린 날로

사장님은 보통 **아침에** 버튼을 누르신다 — 그때 「오늘」은 아직 시공이 없어 늘 실패했다.
`recentPendingDay` 가 최근 14일 안에서 아직 글로 안 쓴 가장 최근 날을 찾아 그날로 만든다.
자동화의 목적이 **밀린 것을 비우는 것**이기 때문이다.

### 실측 (2026-09-02)

| | |
|---|---|
| 원고 1건 | 47~85초 |
| 정가 환산 | $0.12~0.21 (약 170~300원) — **구독이라 실제 청구 0원** |
| API 로 갈아탄다면 | 하루 3건 × 30일 ≈ 월 1.5만~2.7만원 |
| 결과 | 본문 1,400~1,650자, 「속초」 1~2회, 번호판·전화 검출 0 |

### 남은 것 (v3 2~4단계)

사진 폴더 연동 · 본사 제출양식 폼 · 사진 자리표시자가 박힌 5,000자 원고 · 글감 제안.
계획서는 `C:\Users\info\.claude\plans\hidden-weaving-stonebraker.md`.

---

## B단계 — 「사건」을 받는 폼과 촬영 체크리스트 (2026-09-02)

사장님 평가: **"자연스러움이 없으며 ai가 작성한 티가 남."**
나온 글과 사장님이 발행하신 글을 나란히 놓고 원인을 찾았다.

| | 사장님 글 | 프로그램 글 |
|---|---|---|
| 첫 문장 | "쏘렌토 하이브리드로 방문하신 고객님, **이번이 두 번째 방문**이었습니다" | "**여름이 끝나가는 9월 초는** 타이어를 들여다보시기 좋은 시기입니다" |
| 숫자 | `126,726km` · `토우·캠버 기준치 이탈` · `175Nm` | `9만km대` · `십만 원대 안팎` |
| 줄 | 평균 19~21자 | 평균 53~70자 |
| 형식 | 평문 소제목, 표 없음 | `## 소제목` · 마크다운 표 · 체크리스트 |

🔴 **근본 원인은 문체가 아니라 재료였다.** 「차종·규격·본수·계절」뿐이면 쓸 사건이 없어
아무 데나 갖다 붙일 수 있는 일반론으로 문단을 채운다. 그게 「AI 티」의 정체다.

### 세 지렛대 (효과 크기가 다르다)

| | 무엇 | 어디 |
|---|---|---|
| **7할** | **재료** — 왜 오셨나·뭘 봤나·왜 이걸 권했나 + 측정값 | `blog-form.ts`, `/marketing/write` |
| 2할 | **문체 모방** — 규칙 나열 대신 사장님 실제 발행 글 2편을 예시로 | `blog-style.ts` `buildSystem`, `blog-samples.ts` |
| 1할 | **자동 검사** — 마크다운·뭉갠 km·긴 줄·계절로 시작·상투구 → 지적 붙여 재시도 | `blog-style.ts` `styleFilter` |

### 폼 — 빈 칸이 아니라 **칩**

본사 양식 1~8 중 사장님만 아는 4·5·7 을 받되, **누르는 것만으로 채워진다.**
자유 서술은 전부 선택이고, 숫자 칸(남은 홈 mm · 토크 Nm · 공기압 psi · 밸런스 g · CCA)은
넣으면 글이 눈에 띄게 좋아진다. 칩을 하나도 안 고르면 아예 안 만든다 —
재료 없이 만들면 예전 글이 다시 나온다.
폼은 `blog_draft.form` 에 남아 「다르게 한 번 더」가 그대로 다시 쓴다.

### 촬영 체크리스트 (`/marketing/checklist`)

사장님 요청 "정확히 어떤 사진을 몇장 찍어야 하는지". **제가 지어낸 게 아니라 사장님 폴더에서
뽑았다** — 벤츠 GLS 폴더(73장)에 손수 붙이신 `A-00 계기판 주행거리` … `C-06` 의
**A(입고)·B(작업)·C(출고) 3막**이 그대로 뼈대다. 모델3 폴더(24장)가 밀도의 본보기.
실제 발행 글이 쓰는 사진은 12~16장 — 즉 **찍는 건 17~24장, 쓰는 건 12~16장.**
지금 문제는 모자란 게 아니라 **같은 컷이 겹치는 것**이다(임팩트 4장·토크 4장·완성 4장).
정본은 `src/lib/photo-checklist.ts` 한 곳 — 컷을 고치면 화면·자리표시자·검사가 같이 따라간다.

### 실측 — 같은 시공(셀토스 Q26-0901-016) 세 판

| | 원래 | 문체만 | 문체+후기 |
|---|---|---|---|
| 제목 | "첫눈 오고 나서 찾으면 늦습니다" | "9월 초에 겨울 타이어를 먼저 올린 이유" | **"앞 타이어 바깥쪽만 닳아 있었습니다"** |
| 첫 문단 | 일반론 | 사실 나열 | **"첫눈 오기 전에 미리 해 두고 싶다고 하셨습니다"** |
| 소제목 | "여름 끝물에…" | "왜 지금 넣었나" | **"남은 홈 2.8mm" · "밸런스 웨이트 35g"** |
| 길이 / 평균 줄 | 1,649자 / 53자 | 1,383자 / 14자 | **2,162자 / 13자** |
| 마크다운 | 표·체크리스트 | 없음 | 없음 |

사장님 실제 글은 2,456~4,695자 · 평균 줄길이 19~21자 · 마크다운 0 — 그 범위에 들어왔다.

---

## C단계 — 사진 (2026-09-02)

사진 폴더를 앱에서 보고, 고른 사진을 AI 가 **실제로 읽고** 글을 쓴다.
본문에 사진 자리표시자가 박히고, 사장님이 붙여넣기 쉽게 순서대로 복사본까지 만든다.

### 실측 — 쏘렌토 얼라인먼트 건, 사진 12장

AI 가 헌터 장비 화면 사진에서 **수치를 직접 읽어** 본문에 썼다:

> 캠버 좌측 -0.50도, 우측 -0.76도. 토우 좌측 0.6mm, 우측 1.3mm. 캐스터 좌측 4.26도…

계기판 사진에서 읽은 주행거리 **126,726km** 는 DB 값(126,326)이 아니라
**사장님이 실제 발행하신 글의 그 숫자와 정확히 일치**했다 — 사진이 DB 보다 정확했다.

| | 사장님 실제 글 | 프로그램 (사진 12장) |
|---|---|---|
| 제목 | "새 타이어 갈았는데 편마모 날까봐— 속초 쏘렌토 하이브리드 얼라인먼트 재방문 사례" | "새 타이어 갈았는데 소음이 난다면? 속초 쏘렌토 하이브리드 얼라인먼트 재방문" |
| 길이 / 평균 줄 | 2,456자 / 19자 | 2,092자 / 15자 |
| 사진 자리 | 15개 | 12개 |

원고 1건 82초, 정가 환산 $0.80 (글만 쓸 때의 4배 — 사진이 토큰을 먹는다). **구독이라 청구 0원.**

### 지키는 것 세 가지

| | 왜 |
|---|---|
| **원본은 서버로 안 온다** | 폴더 하나가 248MB, 전체 1.3GB. 서버로 오는 건 160px 미리보기(장당 13KB, 전체 2.3MB)뿐이고, 그것도 화면 데이터에 안 싣고 `/api/blog-photo/{id}/thumb` 이 한 장씩 내려 준다 |
| **경로에서 번호판을 지운다** | 폴더 이름이 `264저6834 벤츠 GLS` 다. `--add-dir` 에 원본을 주면 **경로만으로 번호판이 국외로 나간다.** `%TEMP%` 에 `p00.jpg` 로 복사해 그 폴더만 열고, 끝나면 지운다 |
| **사진 없이 조용히 넘어가지 않는다** | 사진을 줬는데 배치 계획이 비면 실패로 보고 다시 쓴다. 가장 나쁜 실패는 실패가 아니라 **사진 없는 글이 그냥 나오는 것**이다 |

### OneDrive 「파일 온디맨드」 — 실측이 예상보다 나빴다

첫 스캔에서 **사진 404장 중 200장이 구름에만** 있었다(폴더 18개 중 11개).
`fs.stat().blocks === 0` 이면 구름 파일이다(실측: 구름 3.7MB인데 blocks=0, 로컬은 blocks=4784).
평소 스캔은 구름 파일을 **안 읽는다** — 읽는 순간 OneDrive 가 통째로 내려받아 스캔이 몇십 분이 된다.
폴더별 「이 폴더 사진 내려받기」를 눌렀을 때만 받아서 미리보기를 만든다.
(전체를 한 번 받아 두면 로컬 1,227MB · 미리보기 404장 완성. 디스크 여유 326GB 라 부담 없음)

### 폴더 ↔ 시공 잇기

폴더명 번호판 → `normalizePlate` → `vehicle.plate_no_norm` → 그 차의 최근 성사 판매.
**18개 중 10개가 자동으로 이어졌다.** 나머지는 차량이 DB 에 없거나(과거 손님) 폴더명에
번호판이 없는 것(`3.4`, `4.6 테슬라 모델Y`) — 화면에서 한 번 고르면 기억한다.

### 정렬 복사본 `_블로그\`

모델이 정한 순서대로 원본 폴더 안에 `A-00 계기판 주행거리 126,726km.jpg` 로 복사한다.
사장님이 벤츠 GLS 폴더에 손수 하시던 방식 그대로다. OneDrive 안이라 **폰에서도 보인다.**

### 여기서 고친 다른 구멍

🔴 **경정비 건이 글감에서 통째로 빠져 있었다.** `factsForDay` 가 「타이어 품목이 있어야 한다」로
걸러서, 얼라인먼트·배터리·TPMS 건이 하나도 안 잡혔다 — 블로그 「경정비 서비스」 카테고리이자
「속초 배터리 교체」처럼 **전화로 가장 빨리 이어지는** 글감인데도. 이제 사장님이 직접 고르신
건은 걸러내지 않고, `custom` 품목(MARS 이관 자리표시자 제외)도 작업명으로 받는다.

---

## D단계 — 이번 주 쓸 글 (2026-09-02)

「뭘 쓰지」가 블로그가 멈추는 가장 흔한 이유다. `/marketing/topics` 가 **근거 넷**으로 골라 준다.

| 우선순위 | 근거 | 왜 |
|---|---|---|
| ① | **밀린 사진 폴더** (`(미업로드)` 먼저) | 사진이 이미 있으니 가장 센 글감 |
| ② | 원고도 폴더도 없는 최근 21일 시공 | 기억이 남아 있을 때 |
| ③ | **차종별 순정 제원** | 시공이 없어도 쓸 수 있어 **글감이 마르지 않는다** |
| ④ | 계절 | 속초 특유의 각도 (미시령·한계령, 관광객) |

🔴 **자리를 나눈다.** 밀린 사진이 늘 몇 건씩 있어 그냥 채우면 정보성 글감이 영영 안 보인다
— 가장 큰 지렛대인데도. 사례 최대 2 · 정보성 1 · 계절 1 로 두어 권장 비중(6:3:1)에 맞춘다.

### 시공 없이 쓰는 정보성 글 (`generateTopicDraft`)

「차종별 순정 제원」은 이 블로그에서 가장 저평가된 카테고리다 — 검색 의도가 분명하고
경쟁이 약한데, **앱에 남이 못 쓰는 근거가 쌓여 있다.**

실측 (제네시스 G80, 시공 7건):

> 속초에서 제네시스 G80은 생각보다 자주 들어옵니다.
> 저희 타이어모어 속초점에서 지금까지 **7대** 작업했습니다.
> 그중 가장 많이 나간 규격은 **245/40R20**이었습니다.

제목: 「G80 타이어 규격, 속초에서 실제로 나가는 사이즈는 245/40R20입니다」 (1,420자 · 평균 줄 17자)

🔴 제원표를 외워 쓰게 하지 않는다 — 「운전석 문 안쪽 스티커와 차량 설명서를 보시라」로
안내하게 지시한다. 트림·옵션마다 다른 값을 지어내면 그 글이 틀린 글이 된다.

### 카테고리 추론 — 폴더 이름이 가장 믿을 만한 단서

처음 붙였을 때 **「포터2 주간등교환」이 수입차 타이어 글로, 「익스플로러 배터리」가
엔진오일로** 잡혔다. 사진 폴더 18개 중 8개는 차량이 앱에 없어(과거 손님) 제조사가 null 이기
때문이다. 그래서 `inferCategory` 가 **폴더 이름**을 같이 본다 — 사장님이 직접 붙이신 이름이라
차종과 작업이 다 들어 있다. 국산 차종 이름 목록도 둬서 제조사를 몰라도 국산을 수입으로
넘기지 않는다. (시험 11개로 실제 폴더 이름을 그대로 검증한다)
