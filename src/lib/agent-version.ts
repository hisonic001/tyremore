/**
 * 매장 PC 대리인의 **판 번호** (2026-09-05, 실제로 크게 데고 만들었다)
 *
 * 🔴 무슨 일이 있었나: 대리인(`scripts/blog-agent.ts`)은 켜 둔 창에서 계속 도는 프로그램이라,
 *    코드를 고쳐도 **다시 켜기 전까지 옛 코드로 돈다.** 그런데 옛 대리인은 모르는 주문을
 *    만나면 **조용히 「원고 만들기」로 흘려보냈다.** 그 결과:
 *      · 「사진 고화질로 준비」를 눌렀더니 사진은 안 만들어지고 **원고 4건이 생겼다**
 *      · 「사진으로 읽기」는 사진을 올려도 **아무 일도 안 일어났다**
 *    사장님은 왜 그런지 알 길이 없었다. 조용한 실패가 가장 나쁘다.
 *
 * 🔴 그래서 대리인이 **자기 판 번호를 알린다.** 앱이 그 번호가 낮으면
 *    단추를 잠그고 「매장 PC 대리인을 다시 켜 주세요」라고 말한다.
 *
 * ⚠️ **대리인이 처리하는 주문 종류를 늘리거나 바꾸면 이 번호를 반드시 올릴 것.**
 *    안 올리면 같은 일이 또 난다.
 *
 * 1 — 처음 (초안·스캔)
 * 2 — 발행사진 추가 (2026-09-05)
 * 3 — 차량사진 추가 · 모르는 주문은 흘려보내지 않고 실패시킴 (2026-09-05)
 * 4 — 차량사진에 판매등록 모드 추가 — 번호판·계기판·등록증 소유자 이름 (2026-09-05)
 * 5 — 번호판 가운데 한글 읽기 지침 강화 — 법정 글자 목록·혼동 쌍 (2026-09-06)
 * 6 — 한글만 모르면 ? 자리 표시(숫자 살리기) — 통째로 비우던 것을 고침 (2026-09-05)
 */
export const AGENT_PROTOCOL = 6;

/** 대리인이 처리할 줄 아는 주문 종류 — 여기 없는 것은 **흘려보내지 않고 실패**시킨다 */
export const KNOWN_JOB_KINDS = ["초안", "스캔", "발행사진", "차량사진"] as const;

export function isKnownJobKind(kind: string): boolean {
  return (KNOWN_JOB_KINDS as readonly string[]).includes(kind);
}

/** 사장님께 보여 줄 말 — 코드 말고 무엇을 하시면 되는지 */
export const OUTDATED_MSG =
  "매장 PC 대리인이 옛 버전입니다 — 그 PC 의 대리인 창을 닫고 다시 켜 주세요 (blog-agent).";
